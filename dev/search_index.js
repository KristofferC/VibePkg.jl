var documenterSearchIndex = {"docs":
[{"category":"section","location":"depots/#Depots","page":"Depots","text":"The depot is where everything VibePkg installs and records lives — by default ~/.julia. Environments reference content in the depot; the depot stores it, exactly once per version, no matter how many environments use it.","title":"Depots"},{"category":"section","location":"depots/#The-depot-stack","page":"Depots","text":"Base.DEPOT_PATH is a stack of depots, configured with the JULIA_DEPOT_PATH environment variable. All depots are searched when looking things up (packages, artifacts, registries), but only the first depot is written to. The last entries are Julia's own bundled depots, which hold the standard library. A typical multi-depot setup is a per-user first depot over a read-only, administrator-maintained shared depot on a multi-user system:\n\nexport JULIA_DEPOT_PATH=\"/home/me/.julia:/opt/julia/shared_depot\"\n\nA trailing empty entry (\"/custom/depot:\") prepends the custom depot while keeping the default entries.","title":"The depot stack"},{"category":"section","location":"depots/#Configuring-the-depot-path","page":"Depots","text":"Reasons to move the depot off its default location:\n\nRoaming user profiles — a depot holds tens of thousands of small files; if the home directory syncs to a server on login/logout, keep the depot out of it.\nQuotas and slow filesystems — home directories on clusters are often small or NFS-mounted; a depot on local scratch is faster and does not eat the quota.\nShared systems — several users can layer their own depot over one centrally maintained depot (see below).\n\nJULIA_DEPOT_PATH is an operating-system environment variable and must be set before Julia starts. On Unix, put an export line in the shell startup file (~/.zshrc, ~/.bashrc, …); on Windows, set it per-session with $env:JULIA_DEPOT_PATH = \"C:\\custom\\depot;\" in PowerShell or permanently through the Environment Variables system dialog.\n\nnote: Note\nKeep the trailing separator (: on Unix, ; on Windows). It expands to the default entries, including the bundled depots that carry the standard library — without it Julia falls back to precompiling the standard library into your depot.\n\nThe depot path can also be set from ~/.julia/config/startup.jl by mutating DEPOT_PATH directly:\n\nempty!(DEPOT_PATH)\npush!(DEPOT_PATH, \"/custom/depot\")\nappend!(DEPOT_PATH, Base.append_default_depot_path!(String[]))\n\nPrefer the environment variable when possible — it takes effect before any code loads. Never rearrange DEPOT_PATH in a running session that has already loaded packages from the old locations.","title":"Configuring the depot path"},{"category":"section","location":"depots/#Shared-depots-on-clusters","page":"Depots","text":"On HPC clusters, pointing every worker at one shared depot avoids downloading and precompiling the same packages once per node. VibePkg serializes conflicting depot writes across processes with pid-file locks, and Julia's precompilation does the same, so many workers can start against a shared depot concurrently — one does the work, the rest wait and reuse it.\n\nTwo caveats:\n\nPrecompilation caches contain native code. If the nodes have different CPU generations, set JULIA_CPU_TARGET to a common baseline (or a multi-target spec) so a cache produced on one node is usable on the others.\nPut the depot on a filesystem where file locking works reliably; some parallel filesystems need their lock manager enabled.","title":"Shared depots on clusters"},{"category":"section","location":"depots/#Shared-depots-on-multi-user-systems","page":"Depots","text":"For lab machines, JupyterHub deployments, and similar setups an administrator can maintain one depot of common packages while users keep installing their own. The scheme is exactly the depot stack from above:\n\nthe user's own depot first (all writes go here),\nthe shared read-only depot second,\nthe bundled depots last (the trailing separator).","title":"Shared depots on multi-user systems"},{"category":"section","location":"depots/#Administrator-setup","page":"Depots","text":"Install into the shared location by making it the first depot while installing:\n\nsudo mkdir -p /opt/julia/shared_depot\nsudo chown $ADMIN /opt/julia/shared_depot\nexport JULIA_DEPOT_PATH=\"/opt/julia/shared_depot:\"\nvpkg add Plots DataFrames CSV\n\nDriving the installation from a committed Project.toml/Manifest.toml and instantiate instead of ad-hoc adds makes the shared set reproducible.\n\nAfterwards, make the depot readable but not writable for users:\n\nsudo chmod -R a+rX,go-w /opt/julia/shared_depot\n\nThe registries/ subdirectory may be deleted from the shared depot: users get their own registry copies in their first depot anyway (only the first depot is written to), so the shared copy is dead weight.","title":"Administrator setup"},{"category":"section","location":"depots/#User-setup","page":"Depots","text":"Each user — or a system-wide profile script such as /etc/profile.d/julia.sh — layers the shared depot into the stack:\n\nexport JULIA_DEPOT_PATH=\"$HOME/.julia:/opt/julia/shared_depot:\"\n\nEverything present in the shared depot is found there; anything else a user adds is downloaded into their own depot. A user who needs a different version of a shared package simply resolves to it — the version in their manifest wins, and it installs into their depot without touching the shared one.\n\nTo pre-seed projects (student labs, container images), publish a template Project.toml/Manifest.toml; users copy it and run instantiate, which only downloads what the shared depot does not already provide.","title":"User setup"},{"category":"section","location":"depots/#Updating-the-shared-depot","page":"Depots","text":"Repeat the administrator setup with up (or an updated manifest plus instantiate). New versions install alongside old ones, so users whose manifests reference the old versions are unaffected. If old versions are garbage-collected from the shared depot to save space, affected users can instantiate to re-download them into their own depot.","title":"Updating the shared depot"},{"category":"section","location":"depots/#Troubleshooting","page":"Depots","text":"If shared packages are not found, inspect the stack from Julia — it shows exactly what the environment variable expanded to:\n\njulia> DEPOT_PATH\n3-element Vector{String}:\n \"/home/user/.julia\"\n \"/opt/julia/shared_depot\"\n \"/usr/local/share/julia\"\n\nThe usual causes are a missing trailing separator or the variable not being set in the environment the Julia process actually starts from (e.g. a batch job that does not source the profile script).","title":"Troubleshooting"},{"category":"section","location":"depots/#Layout","page":"Depots","text":"The subdirectories of a depot, all managed by VibePkg unless noted:\n\nDirectory Contents\npackages/Name/slug/ installed package source trees, one per version, read-only\nartifacts/<treehash>/ installed artifact trees, content-addressed, read-only\nregistries/ installed registries (packed tarballs or unpacked trees)\nclones/ bare git clones cached for repo-tracked packages\nenvironments/ shared environments, incl. the default v1.x and apps/\ndev/ default target of develop (JULIA_PKG_DEVDIR overrides)\nscratchspaces/ per-package mutable scratch storage, incl. build.log files\nlogs/ usage logs (which manifests/artifacts have been used) and REPL history\nbin/ executable shims for installed Apps\nservers/ package-server authentication (auth.toml)\ncompiled/ precompilation caches (written by Julia itself)\n\nThe five-character slug in packages/Example/AqEK9/ distinguishes versions (it hashes the package UUID and content tree), which is how many versions of one package coexist.\n\nInstalled trees are immutable: packages/ and artifacts/ content is made read-only on installation and verified by content hash. Anything mutable a package needs at run time belongs in a scratchspace.\n\nThe usage logs in logs/ are what gc uses to decide reachability: every environment load appends the manifest path, and every artifact use appends the Artifacts.toml path. When those files no longer exist, the content they kept alive becomes collectable — see Garbage collecting old, unused packages.\n\ncompiled/ is worth an occasional manual look: caches for Julia versions you no longer run are never cleaned up by those Julias, so deleting stale compiled/v1.x subdirectories is safe and can reclaim real space.","title":"Layout"},{"category":"section","location":"devdocs/timing/#Timing-instrumentation","page":"Timing instrumentation","text":"VibePkg's hot paths are instrumented with TimerOutputs through the Timing module (src/Timing.jl).","title":"Timing instrumentation"},{"category":"section","location":"devdocs/timing/#Enabling","page":"Timing instrumentation","text":"Timing is disabled by default: TimerOutputs is not a dependency of VibePkg and its load in src/Timing.jl is commented out — @timeit and @operation are no-op fallbacks and every instrumentation site compiles unchanged with zero overhead. To enable it:\n\nAdd TimerOutputs to the VibePkg project explicitly (pkg> add TimerOutputs with the VibePkg environment active).\nUncomment the using TimerOutputs: ... line at the top of src/Timing.jl.","title":"Enabling"},{"category":"section","location":"devdocs/timing/#Usage","page":"Timing instrumentation","text":"Timing.TIMER is the global timer. Every public entry point in API.jl is wrapped in Timing.@operation: the outermost operation of a call tree resets and starts the timer on entry and stops it (disable_timer!) on exit, so after any operation TIMER holds exactly that operation's timings and instrumented code running outside an operation records nothing:\n\njulia> VibePkg.up();\n\njulia> VibePkg.Timing.TIMER\n──────────────────────────────────────────────────────────────────────────────\n                                    Time                    Allocations\n                           ───────────────────────   ────────────────────────\n     Tot / % measured:          1.52s /  98.1%           98.3MiB /  95.4%\n ...\n\nTo print the report automatically when an operation returns, call VibePkg.Timing.print_timings(true) or set JULIA_PKG_TIMING=true.","title":"Usage"},{"category":"section","location":"devdocs/timing/#Instrumenting-more-code","page":"Timing instrumentation","text":"Annotate a function whose cost is significant or interesting with @timeit TIMER \"label\" function foo(...) ... end (import both names with using ..Timing: @timeit, TIMER), or wrap an individual call site.\nWrap a new public entry point with Timing.@operation so it participates in the reset-on-entry/print-on-exit protocol. Nested operations only open a timer section; a label can be passed explicitly (@operation \"rm\" function _rm_requests(...)) and defaults to the function name.\nTimerOutputs is not task-safe: never put @timeit inside the concurrent download tasks (the @async batches in Execution.jl) — instrument the function that owns the @sync instead.","title":"Instrumenting more code"},{"category":"section","location":"protocol/#Pkg-Server-Protocols","page":"Package server protocols","text":"By default VibePkg downloads registries, packages, and artifacts from a package server over plain HTTPS instead of git-cloning each package's repository. Two protocols are involved: the Pkg protocol, spoken between the client (VibePkg) and a package server, and the storage protocol, spoken between package servers and the storage services behind them. This page describes both from the client's point of view — for day-to-day configuration see Package servers and the environment variables reference.\n\nThe package-server design fixes several problems with fetching packages straight from their hosting:\n\nPermanence. Authors delete repositories, and artifacts often live on plain web servers with no versioning at all. A storage service keeps every version it has ever served, so old manifests stay instantiable.\nFirewalls. All traffic is HTTPS GET/HEAD against one host, instead of git plus assorted protocols against an unbounded set of hosts.\nNo git dependency. git is only needed for unregistered packages, repo-tracked (add --url) packages, and git-form registries.\nEcosystem insight. The public servers give the Julia community aggregate download statistics that proprietary code hosts do not share.","title":"Package server protocols"},{"category":"section","location":"protocol/#Architecture","page":"Package server protocols","text":"client ──Pkg protocol──▶ package server ──storage protocol──▶ storage services ──▶ GitHub, …\n\nThe client talks to one package server: https://pkg.julialang.org (public, unauthenticated, community-run) unless JULIA_PKG_SERVER names another. Anyone can run one — servers are stateless caches and scale horizontally; a reference implementation is PkgServer.jl.\n\nA package server is backed by one or more storage services, which fetch resources from the original hosts (GitHub, GitLab, …) and persist everything they have ever served to durable storage. Multiple independent storage services can back the same package server, for redundancy and so that no single operator controls the ecosystem's package supply. The storage protocol is server-to-server only — clients never speak it — and uses mutually authenticated TLS, in contrast to the Pkg protocol, whose authentication is optional and token-based (below). Storage services promise persistence (anything served once can be served forever) and aim for completeness (a served registry's packages, and a served package's artifacts, are fetched and kept as well).","title":"Architecture"},{"category":"section","location":"protocol/#Resources","page":"Package server protocols","text":"The client issues GET requests for four kinds of resource:\n\nResource Contents\n/registries one line per registry: /registry/$uuid/$hash for the current version of each registry the server offers\n/registry/$uuid/$hash tarball of that registry snapshot\n/package/$uuid/$hash tarball of a package source tree\n/artifact/$hash tarball of an artifact tree\n\nEverything except /registries is addressed by content hash and therefore immutable — servers and clients cache those forever. Updating a registry means fetching /registries and comparing hashes against the installed snapshot (this is what registry update does with a package server).\n\nTarballs may be zstd- or gzip-compressed. VibePkg checks the downloaded file's magic bytes rather than trusting its filename or response headers before decompressing it.\n\nEvery request to the package server (and only to it) carries a set of identifying headers: the protocol version, the client's Julia version and platform triplet, whether the session is interactive, and a summary of CI-related environment variables — this is the basis of the public download statistics. Any JULIA_PKG_SERVER_* environment variable is forwarded as a Julia-* header (JULIA_PKG_SERVER_REGISTRY_PREFERENCE → Julia-Registry-Preference), which is how server-side options like registry flavors are selected.","title":"Resources"},{"category":"section","location":"protocol/#Verification-and-fallback","page":"Package server protocols","text":"All resources are content-addressed by git tree hash. After download and extraction, VibePkg recomputes the tree hash and refuses content that does not match — a corrupt or tampered source is skipped, not installed. For packages, candidate sources are tried in order: the package server, then a GitHub archive URL synthesized from the package's repository, then a full git clone. JULIA_PKG_SERVER=\"\" disables the package server entirely, and offline mode skips downloads altogether.","title":"Verification and fallback"},{"category":"section","location":"protocol/#Authentication","page":"Package server protocols","text":"The public server is anonymous, but a private package server can require authorization. The Pkg protocol uses RFC 6750 bearer tokens: authenticated requests carry an Authorization: Bearer $access_token header. How tokens are issued and what they contain is entirely the server operator's business; the client only stores, sends, and refreshes them.","title":"Authentication"},{"category":"section","location":"protocol/#auth.toml","page":"Package server protocols","text":"Credentials live in the first depot at\n\n~/.julia/servers/<host>/auth.toml\n\n(one directory per server host; characters that are invalid in directory names, such as the : in host:port, become _). The user or a user agent (IDE, company tooling) is responsible for putting the initial file there. VibePkg reads these fields:\n\naccess_token (required) — the bearer token sent with requests\nexpires_at (optional) — absolute expiry, seconds since epoch\nexpires_in (optional) — relative expiry, seconds\nrefresh_token (optional) — token authorizing refresh requests\nrefresh_url (optional) — where to fetch a fresh auth.toml\n\nOther fields are preserved but ignored. The effective expiry is the minimum of expires_at and mtime(auth.toml) + expires_in; keeping the relative form tied to the file's modification time makes expiry robust against clock skew between server and client.","title":"auth.toml"},{"category":"section","location":"protocol/#Token-refresh","page":"Package server protocols","text":"Bearer tokens are typically short-lived, so VibePkg refreshes them automatically. Starting ten minutes before expiry, each package-server request first performs a refresh: a GET of refresh_url with Authorization: Bearer $refresh_token, whose response must be a new auth.toml (at minimum an access_token). The new file is written back atomically with owner-only permissions, converting a relative expires_in into an absolute expires_at based on the client clock. refresh_url must be HTTPS (plain HTTP is accepted only for localhost development); an expired token that cannot be refreshed is simply not sent, so the request proceeds anonymously.\n\nIf the server answers 401 Unauthorized anyway (a token revoked server-side, say), VibePkg forces one refresh and retries the request once. A second 401 raises an error that includes the response body, so a server can explain — in plain text — what the user should do.","title":"Token refresh"},{"category":"section","location":"protocol/#Authentication-hooks","page":"Package server protocols","text":"A user agent can register a handler to run whenever VibePkg fails to produce a usable token, and for example open a login flow that writes a fresh auth.toml:\n\ndispose = VibePkg.Fetch.register_auth_error_handler(\n    \"https://pkg.company.com\"\n) do url, server, err\n    MyAuthTool.login(server)      # acquire and write auth.toml\n    return true, true             # handled; retry the token lookup\nend\n\n# ... later ...\ndispose()\n\nThe first argument selects which URLs the handler applies to (a substring or Regex, matched with occursin). The handler receives the failing URL, the configured package server, and an error code — one of \"no-auth-file\", \"malformed-file\", \"no-access-token\", \"no-refresh-key\", or \"insecure-refresh-url\" — and returns (handled, should_retry): when handled is true no further handlers run, and should_retry re-runs the token lookup once, picking up whatever the handler wrote. Handlers are tried most-recently-registered first. register_auth_error_handler returns a zero-argument deregistration function; the same effect is available as VibePkg.Fetch.deregister_auth_error_handler(urlscheme, f).","title":"Authentication hooks"},{"category":"section","location":"protocol/#Storage-service-interaction","page":"Package server protocols","text":"For a content-addressed resource, a package server first sends HEAD requests to its configured storage services and downloads with GET from one that reports the resource. A successful HEAD response is itself a commitment by that storage service to retain the resource, even if another service supplies the bytes for this particular request.\n\nThe changing /registries endpoint needs one extra rule because tree hashes have no natural ordering. When storage services report different current hashes, the package server asks each service whether it knows the others' hashes. A service that knows another snapshot and also reports a different one has the later snapshot; genuinely divergent snapshots can be broken as a tie.\n\nA storage service's hard guarantee is persistence: after successfully serving a registry, package, or artifact tree, it must continue to serve that exact tree. Completeness is best effort rather than absolute — a registry can contain an erroneous or already-vanished package hash — but storage services should eagerly fetch all newly referenced package trees and artifacts so the stored ecosystem is as closed and reproducible as possible.","title":"Storage-service interaction"},{"category":"section","location":"artifacts/#Artifacts","page":"Artifacts","text":"Artifacts are how packages ship data that isn't Julia code: compiled libraries, datasets, fonts, models. An artifact is an immutable directory tree, identified by the git tree hash of its contents and stored content-addressed in the depot (~/.julia/artifacts/<treehash>), so identical artifacts are stored once no matter how many packages or versions reference them.\n\nPackages declare their artifacts in an Artifacts.toml file at the package root, and access them with the artifact\"name\" string macro from the Artifacts standard library. VibePkg's role is the management side: downloading and installing the right artifacts when a package is installed, verifying them by hash, honoring overrides, and garbage-collecting unused trees.\n\nAs with the project and manifest files, JuliaArtifacts.toml is accepted as an alternative name. When both names exist, the Julia-prefixed file takes precedence.","title":"Artifacts"},{"category":"section","location":"artifacts/#Artifacts.toml","page":"Artifacts","text":"A minimal entry binds a name to a tree hash and tells where a tarball of the content can be downloaded:\n\n[socrates]\ngit-tree-sha1 = \"43563e7631a7eafae1f9f8d9d332e3de44ad7239\"\n\n    [[socrates.download]]\n    url = \"https://github.com/staticfloat/small_bin/raw/master/socrates.tar.gz\"\n    sha256 = \"e65d2f13f2085f2c279830e863292312a72930fee5ba3c792b14c33ce5c5cc58\"\n\nThe package can then do:\n\nusing Artifacts\n\nfunction load_socrates()\n    rootpath = artifact\"socrates\"\n    return open(joinpath(rootpath, \"bin\", \"socrates\")) do io\n        read(io, String)\n    end\nend\n\nThe git-tree-sha1 identifies the unpacked content and is what gets verified after download; the sha256 verifies the tarball itself. Multiple [[name.download]] stanzas give mirror URLs, tried in order until one downloads and verifies successfully.\n\nTwo attributes and one structural variation extend the format:\n\nlazy = true — the artifact is not downloaded when the package is installed, only on first use (artifact\"...\" triggers the download).\nPlatform-specific artifacts — instead of a single table, an array of tables keyed by platform properties; the entry matching the host is selected:\n\n[[c_simple]]\narch = \"x86_64\"\ngit-tree-sha1 = \"4bdf4556050cb55b67b211d4e78009aaec378cbc\"\nlibc = \"musl\"\nos = \"linux\"\n\n    [[c_simple.download]]\n    sha256 = \"411d6befd49942826ea1e59041bddf7dbb72fb871bb03165bf4e164b13ab5130\"\n    url = \"https://github.com/JuliaBinaryWrappers/c_simple_jll.jl/releases/download/c_simple%2Bv1.2.3%2B7/c_simple.v1.2.3.x86_64-linux-musl.tar.gz\"\n\n[[c_simple]]\narch = \"x86_64\"\ngit-tree-sha1 = \"51264dbc770cd38aeb15f93536c29dc38c727e4c\"\nos = \"macos\"\n\n    [[c_simple.download]]\n    sha256 = \"6c17d9e1dc95ba86ec7462637824afe7a25b8509cc51453f0eb86eda03ed4dc3\"\n    url = \"https://github.com/JuliaBinaryWrappers/c_simple_jll.jl/releases/download/c_simple%2Bv1.2.3%2B7/c_simple.v1.2.3.x86_64-apple-darwin14.tar.gz\"\n\nNo download stanza — an entry with only a hash names content that is produced locally (or provided some other way); nothing is downloaded for it.\n\nTo compute the git-tree-sha1 and sha256 for a tarball you are binding by hand:\n\nusing Tar, Inflate, SHA\nfilename = \"socrates.tar.gz\"\nprintln(\"sha256: \", bytes2hex(open(sha256, filename)))\nprintln(\"git-tree-sha1: \", Tar.tree_hash(IOBuffer(inflate_gzip(filename)), algorithm = \"git-sha1\"))\n\nIn practice most Artifacts.toml files are machine-generated: binary-providing _jll packages by BinaryBuilder.jl, and hand-bound data artifacts conveniently with ArtifactUtils.jl.","title":"Artifacts.toml"},{"category":"section","location":"artifacts/#Installation-and-verification","page":"Artifacts","text":"When a package is installed or an environment is instantiated, VibePkg walks each package's Artifacts.toml, selects the entries matching the host platform, and downloads all non-lazy artifacts (from the package server when configured, otherwise from the listed URLs), unpacking each and verifying the tree hash before the artifact becomes visible. Artifact trees are installed read-only.\n\nTree-hash computation depends on the file system: on Windows without symlink permissions the hash of a symlink-containing artifact cannot reproduce, and the mismatch is automatically downgraded to a warning there. Setting the environment variable JULIA_PKG_IGNORE_HASHES=1 forces that lenient behavior everywhere (and JULIA_PKG_IGNORE_HASHES=0 disables it even on Windows) — use with care, since it accepts content whose hash does not match what the package declared.\n\nLazy artifacts can be pre-installed programmatically, which is useful in deployment images:\n\nusing VibePkg.Artifacts\nensure_artifact_installed(\"socrates\", find_artifacts_toml(pathof(MyPkg)))","title":"Installation and verification"},{"category":"section","location":"artifacts/#Querying-and-managing-artifacts-from-code","page":"Artifacts","text":"VibePkg.Artifacts re-exports the query functions of the Artifacts standard library — artifact_meta, artifact_hash, artifact_exists, artifact_path, find_artifacts_toml, select_downloadable_artifacts — and adds the management side:\n\nensure_artifact_installed(name, artifacts_toml; platform = HostPlatform()) -> path — install the named artifact if it is missing (lazy or not) and return its path.\nverify_artifact(hash) -> Bool — whether the artifact tree for hash exists and its content matches the hash.\nremove_artifact(hash) — delete the artifact tree (normally you let gc do this: it removes artifacts no Artifacts.toml in use references anymore).","title":"Querying and managing artifacts from code"},{"category":"section","location":"artifacts/#Creating-artifacts-from-code","page":"Artifacts","text":"Artifacts can also be produced programmatically — the typical use is a script in a package's repository that regenerates a dataset artifact. create_artifact materializes a new artifact tree: it hands your callback a fresh directory to fill in, then hashes the result and moves it into the depot. bind_artifact! records the resulting hash under a name in an Artifacts.toml. Querying before creating makes the script idempotent:\n\nusing VibePkg.Artifacts\n\nartifacts_toml = joinpath(@__DIR__, \"Artifacts.toml\")\n\nhash = artifact_hash(\"dataset\", artifacts_toml)\nif hash === nothing || !artifact_exists(hash)\n    hash = create_artifact() do dir\n        # fill `dir` with the artifact's content\n        write(joinpath(dir, \"data.csv\"), fetch_the_data())\n    end\n    bind_artifact!(artifacts_toml, \"dataset\", hash)\nend\n\nartifact_path(hash)\n\nbind_artifact! accepts the same variations the file format has: a platform keyword for platform-specific bindings (one entry per platform), download_info — a vector of (url, sha256) or (url, sha256, size) tuples — to write the download stanzas that let others install the artifact, lazy = true, and force = true to replace an existing binding. unbind_artifact! removes a binding from the file.","title":"Creating artifacts from code"},{"category":"section","location":"artifacts/#Overriding-artifact-locations","page":"Artifacts","text":"Sometimes an artifact should come from somewhere else — a system library instead of the shipped one, a locally built binary, a shared network copy. A depot-wide ~/.julia/artifacts/Overrides.toml redirects artifacts, either by hash or per package and name:\n\n# Override by content hash: to a path, or to another artifact's hash\n78f35e74ff113f02274ce60dab6e92b4546ef806 = \"/path/to/replacement\"\nc76f8cda85f83a06d17de6c57aabf9e294eb2537 = \"fb886e813a4aed4147d5979fcdf27457d20aa35d\"\n\n# Override by package UUID and artifact name\n[d57dbccd-ca19-4d82-b9b8-9d660942965b]\nc_simple = \"/path/to/c_simple_dir\"\nlibfoo = \"fb886e813a4aed4147d5979fcdf27457d20aa35d\"\n\nOverridden artifacts are never downloaded; artifact_path and artifact\"...\" resolve to the override target. An empty string removes an override.\n\nEvery depot may provide an Overrides.toml. They are layered in depot order, with the first depot taking precedence; an empty-string entry in a higher priority file can therefore re-enable the original artifact after a lower priority depot overrode it. Name-based overrides require the package UUID as well as the artifact name; the string macro supplies that context automatically.","title":"Overriding artifact locations"},{"category":"section","location":"artifacts/#Extending-platform-selection","page":"Artifacts","text":"For artifacts whose correct variant depends on more than the base platform triplet (CUDA version, CPU microarchitecture, …), a package can augment the platform used for selection by shipping a .pkg/select_artifacts.jl hook next to its Artifacts.toml. VibePkg runs it at install time to ask the package which artifacts to download; at runtime the package passes the same augmented platform to @artifact_str. See the Julia manual on Base.BinaryPlatforms for how platform augmentation is written.\n\nThe hook receives the target platform triplet as ARGS[1] and must print a TOML dictionary in the same shape as select_downloadable_artifacts(artifacts_toml; platform). It runs in a minimal subprocess whose load path contains only Base and the standard libraries, so it must not import the package it is selecting artifacts for or any third-party dependency. Keep the hook lightweight: it runs during package operations, not only when the package is loaded.","title":"Extending platform selection"},{"category":"section","location":"toml-files/#Project-and-Manifest","page":"Project.toml and Manifest.toml","text":"Every environment is described by up to two TOML files. Project.toml is the high-level, hand-editable description: dependencies, compatibility, package metadata. Manifest.toml is the machine-generated record of an exact resolution: every package in the dependency graph with its version and origin. Both are written deterministically (sorted, stable formatting), so they diff well under version control.\n\nIf a JuliaProject.toml exists it takes precedence over Project.toml (useful when another tool claims the name Project.toml), and pairs with JuliaManifest.toml.","title":"Project.toml and Manifest.toml"},{"category":"section","location":"toml-files/#Project.toml","page":"Project.toml and Manifest.toml","text":"","title":"Project.toml"},{"category":"section","location":"toml-files/#name","page":"Project.toml and Manifest.toml","text":"The name of the package. Must be a valid Julia identifier. Required for a package (together with uuid); optional for a plain environment. For choosing a good one, see Package naming rules.","title":"name"},{"category":"section","location":"toml-files/#uuid","page":"Project.toml and Manifest.toml","text":"A UUID identifying the package independent of its name. Required for a package; generate creates one, or produce one manually with using UUIDs; uuid4(). Never change it after registration — identity, not the name, is what the rest of the ecosystem depends on.","title":"uuid"},{"category":"section","location":"toml-files/#version","page":"Project.toml and Manifest.toml","text":"The package's version, e.g. version = \"1.2.5\". Interpreted by semantic versioning, with the pre-1.0 rules described in Compatibility.","title":"version"},{"category":"section","location":"toml-files/#authors","page":"Project.toml and Manifest.toml","text":"A TOML array whose entries are either \"NAME\" / \"NAME <EMAIL>\" strings or tables following the Citation File Format person/entity schema. Tables are useful for structured names and identifiers:\n\nauthors = [\n    \"Some One <someone@example.com>\",\n    {given-names = \"Ada\", family-names = \"Lovelace\", orcid = \"https://orcid.org/0000-0000-0000-0000\"},\n]","title":"authors"},{"category":"section","location":"toml-files/#readonly","page":"Project.toml and Manifest.toml","text":"Setting readonly = true marks the environment as immutable: operations that would modify it error instead, and status shows a (readonly) suffix. Useful for environments that are deployed or shared and must not drift.","title":"readonly"},{"category":"section","location":"toml-files/#[deps]","page":"Project.toml and Manifest.toml","text":"The direct dependencies, as Name = \"UUID\" pairs:\n\n[deps]\nExample = \"7876af07-990d-54b4-ab0e-23690620f79a\"","title":"[deps]"},{"category":"section","location":"toml-files/#[sources]","page":"Project.toml and Manifest.toml","text":"Where to get specific dependencies from, when it isn't a registry: a path, or a url with an optional revision and subdirectory.\n\n[sources]\nExample = {url = \"https://github.com/JuliaLang/Example.jl\", rev = \"master\"}\nWithinMonorepo = {url = \"https://example.com/BigProject.git\", subdir = \"packages/WithinMonorepo\"}\nSomeDependency = {path = \"deps/SomeDependency.jl\"}\n\nurl and path are mutually exclusive; rev and subdir refine a URL source. Sources override registry information for the direct dependencies of the active project. They are a development convenience, not metadata that ordinary consumers of a registered package inherit.\n\nThere is one intentional exception: when a package itself is added by URL or path, VibePkg recursively follows that package's own [sources] entries. This allows a chain of unregistered or private packages to describe where its dependencies come from without a registry. See Private dependency trees with [sources] for a complete example. Sources are written automatically when you add a URL/local repository or develop a package.","title":"[sources]"},{"category":"section","location":"toml-files/#[compat]","page":"Project.toml and Manifest.toml","text":"Version requirements for the entries in [deps], [weakdeps], and [extras], plus julia. See Compatibility for the syntax.","title":"[compat]"},{"category":"section","location":"toml-files/#[weakdeps]-and-[extensions]","page":"Project.toml and Manifest.toml","text":"Weak dependencies are packages your package can cooperate with without requiring; extensions are the modules that load when they appear. See Weak dependencies and extensions in Creating Packages.\n\n[weakdeps]\nContour = \"d38c429a-6771-53c6-b99e-75d170b6e991\"\n\n[extensions]\nContourExt = \"Contour\"","title":"[weakdeps] and [extensions]"},{"category":"section","location":"toml-files/#[workspace]","page":"Project.toml and Manifest.toml","text":"Declares a workspace by listing the member projects (relative paths):\n\n[workspace]\nprojects = [\"test\", \"docs\"]\n\nAll members share the manifest of the workspace root. See Workspaces.","title":"[workspace]"},{"category":"section","location":"toml-files/#[extras]-and-[targets]-(legacy)","page":"Project.toml and Manifest.toml","text":"The older mechanism for test-only dependencies:\n\n[extras]\nTest = \"8dfed614-e22c-5e08-85e1-65c5234f0b40\"\n\n[targets]\ntest = [\"Test\"]\n\nSupported, but the workspace-based test project is preferred — see Adding tests.\n\nAny other top-level keys are preserved verbatim across operations, so tools may store their own configuration in the project file.","title":"[extras] and [targets] (legacy)"},{"category":"section","location":"toml-files/#Manifest.toml","page":"Project.toml and Manifest.toml","text":"The manifest is machine-generated; editing it by hand is not advised. Its top-level fields:\n\njulia_version = \"1.12.0\"\nmanifest_format = \"2.1\"\nproject_hash = \"2ca1c6c58cb30e79e021fb54e5626c96d05d5fdc\"\n\njulia_version — the Julia that produced the resolution. instantiate warns (or errors with --julia_version_strict) when it doesn't match the running Julia at minor-version granularity.\nmanifest_format — the file format version. VibePkg writes 2.1; format 1 manifests (pre-Julia-1.7, flat layout) are still read, with a warning, and are upgraded the next time the environment is resolved.\nproject_hash — a hash of the project file contents the manifest was resolved against; how VibePkg detects that project and manifest are out of sync.\n\nA [registries] table records which registries provided the resolution (format 2.1):\n\n[registries.General]\nuuid = \"23338594-aafe-5451-b93e-139f81909106\"\nurl = \"https://github.com/JuliaRegistries/General.git\"\n\nThe UUID is required; the optional URL records where the registry came from.","title":"Manifest.toml"},{"category":"section","location":"toml-files/#Package-entries","page":"Project.toml and Manifest.toml","text":"Each package in the graph gets one entry under [[deps.Name]]. Which fields appear depends on how the package is tracked:\n\n# Registered package at a released version\n[[deps.Example]]\ngit-tree-sha1 = \"46e44e869b4d90b96bd8ed1fdcf32244fddfb6cc\"\nregistries = \"General\"\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nversion = \"0.5.5\"\n\n# Tracking a git branch (or commit — repo-rev is then the SHA)\n[[deps.Example]]\ngit-tree-sha1 = \"...\"\nrepo-rev = \"master\"\nrepo-url = \"https://github.com/JuliaLang/Example.jl.git\"\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nversion = \"0.5.6\"\n\n# With dependencies of its own\n[[deps.Foo]]\ndeps = [\"Example\"]\ngit-tree-sha1 = \"...\"\nuuid = \"...\"\nversion = \"1.0.0\"\n\n# Developed (tracking a path)\n[[deps.Example]]\npath = \"/home/user/.julia/dev/Example\"\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nversion = \"0.5.6\"\n\n# Pinned\n[[deps.Example]]\ngit-tree-sha1 = \"...\"\npinned = true\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nversion = \"0.5.5\"\n\ndeps lists the entry's own dependencies by name (as a nested table with UUIDs in the rare case that two packages in the manifest share a name), and git-tree-sha1 is the content hash the installation is verified against. For registry-tracked entries, registries names the [registries] records that supplied that version; it is an array when several registries contain it.","title":"Package entries"},{"category":"section","location":"toml-files/#Packages-with-the-same-name","page":"Project.toml and Manifest.toml","text":"Package identity is the UUID, not the name, so one manifest can contain two different packages both called B. In that case [[deps.B]] appears twice, and a package depending on one of them expands its dependency list to a name-to-UUID table:\n\n[[deps.A]]\nuuid = \"ead4f63c-334e-11e9-00e6-e7f0a5f21b60\"\n\n    [deps.A.deps]\n    B = \"f41f7b98-334e-11e9-1257-49272045fb24\"\n\n[[deps.B]]\nuuid = \"f41f7b98-334e-11e9-1257-49272045fb24\"\n\n[[deps.B]]\nuuid = \"edca9bc6-334e-11e9-3554-9595dbb4349c\"","title":"Packages with the same name"},{"category":"section","location":"toml-files/#Versioned-manifests","page":"Project.toml and Manifest.toml","text":"A file named Manifest-v1.12.toml (for example) is preferred over Manifest.toml when running Julia 1.12, letting one project carry resolutions for several Julia versions side by side. VibePkg honors such files when they exist but never creates them itself.","title":"Versioned manifests"},{"category":"section","location":"apps/#Apps","page":"Apps","text":"An app is a Julia package installed so that its entry point becomes an executable on your PATH — you run it from the terminal like any other program:\n\n$ reverse some input string\nemos tupni gnirts\n\nInstalling an app gives it a dedicated, isolated environment in the depot, so apps never conflict with your projects or with each other, plus a small executable shim in ~/.julia/bin that starts Julia with the right environment and main module.\n\nwarning: Warning\nApps are experimental — functionality and API may change.","title":"Apps"},{"category":"section","location":"apps/#Creating-a-Julia-app","page":"Apps","text":"An app is an ordinary package whose module defines an entry point with @main, and whose Project.toml declares the executables in an [apps] table:\n\nmodule MyReverseApp\n\nfunction (@main)(ARGS)\n    for arg in ARGS\n        print(stdout, reverse(arg), \" \")\n    end\n    return\nend\n\nend # module\n\nname = \"MyReverseApp\"\nuuid = \"...\"\nversion = \"0.1.0\"\n\n[apps]\nreverse = {}\n\nAfter app add, the command reverse is available in the terminal.","title":"Creating a Julia app"},{"category":"section","location":"apps/#Multiple-apps-per-package","page":"Apps","text":"One package can provide several executables by putting the extra entry points in submodules:\n\n[apps]\nmain-app = {}\ncli-app = { submodule = \"CLI\" }\n\nmain-app runs julia -m MyMultiApp; cli-app runs julia -m MyMultiApp.CLI, i.e. the @main defined in the CLI submodule (dotted names like CLI.Nested work too).","title":"Multiple apps per package"},{"category":"section","location":"apps/#Julia-flags","page":"Apps","text":"Each app can declare the Julia flags it should run under:\n\n[apps]\nmyapp = { julia_flags = [\"--threads=4\", \"--optimize=2\"] }\ndebug-app = { submodule = \"Debug\", julia_flags = [\"--check-bounds=yes\", \"--optimize=0\"] }\n\nAt run time, flags can be overridden per invocation: arguments before a -- separator go to Julia, the rest to the app.\n\n$ myapp --threads=8 -- input.txt output.txt\n\nThe environment variable JULIA_APPS_JULIA_CMD overrides which julia executable the shims use (by default, the one that installed the app).","title":"Julia flags"},{"category":"section","location":"apps/#Installing-apps","page":"Apps","text":"Apps are managed with the app REPL commands (or VibePkg.Apps.*):\n\n(@v1.12) vpkg> app add Runic   # a registered package, by name\n\napp add builds the app's private environment under ~/.julia/environments/apps/<PkgName>, resolves and precompiles it, and writes the shims. For working on an app, app develop points the shims directly at a source tree instead, so edits take effect on the next run without reinstalling:\n\n(@v1.12) vpkg> app develop path/to/Package\n\napp update refreshes installed registered packages to their latest resolvable versions and re-resolves developed apps in place. With no argument it updates everything; a package or app name restricts the operation:\n\n(@v1.12) vpkg> app update\n(@v1.12) vpkg> app update runic\n\napp status lists installed app packages and their executables. It accepts package or app names as optional filters. app rm removes a package with all its apps, or a single named app:\n\n(@v1.12) vpkg> app status\n[abc12345] MyReverseApp v0.1.0\n  reverse\n\n(@v1.12) vpkg> app status reverse\n\n(@v1.12) vpkg> app rm MyReverseApp\n\nnote: Note\nVibePkg never modifies your shell configuration. If ~/.julia/bin is not on your PATH, a warning reminds you to add it:export PATH=\"$HOME/.julia/bin:$PATH\"","title":"Installing apps"},{"category":"section","location":"compatibility/#Compatibility","page":"Compatibility","text":"The [compat] table in a Project.toml declares which versions of your dependencies (and of Julia itself) the project is known to work with. The resolver never selects a version outside the declared ranges:\n\n[deps]\nExample = \"7876af07-990d-54b4-ab0e-23690620f79a\"\n\n[compat]\nExample = \"0.5\"\njulia = \"1.12\"\n\nCompat entries apply to [deps], [weakdeps], and [extras]; julia may always be given even though it is not a listed dependency.\n\nWith no compat entry, every version is allowed. That is convenient while exploring, but published packages should state the versions they have actually tested: a missing upper bound lets a future breaking release enter the resolution.","title":"Compatibility"},{"category":"section","location":"compatibility/#compat-on-add","page":"Compatibility","text":"When the active project is a package — its Project.toml has both a name and uuid — adding a direct dependency automatically creates a compat entry beginning at the version selected by the resolver:\n\n(MyPackage) vpkg> add Example\n   Resolving package versions...\n     Compat entries added for Example\n\nAn existing entry is never overwritten, including when the dependency is re-added. Plain environments without a package name and UUID do not get an automatic entry. The generated constraint is a safe starting point, not a claim that every allowed version has been tested; package authors should still review their bounds before publishing.\n\nThe easiest way to maintain the table is the compat command:\n\n(@v1.12) vpkg> compat                # show the table\n(@v1.12) vpkg> compat Example 0.5    # set an entry (and re-check the env)\n(@v1.12) vpkg> compat Example        # remove an entry\n(@v1.12) vpkg> compat --current      # fill missing entries from resolved versions\n\nSetting an entry that conflicts with the currently resolved versions keeps the entry and suggests running update — the environment is never silently downgraded.\n\ninfo: Info\nPackages registered in the General registry are required to have upper-bounded compat entries for all their dependencies.The syntax on this page is for a project's Project.toml. Registry Compat.toml files use a different range syntax described in Registry format.","title":"Compatibility entries created by add"},{"category":"section","location":"compatibility/#Version-specifiers","page":"Compatibility","text":"Compat entries use semantic versioning, with one adjustment for pre-1.0 versions described below. A bare version number is a caret specifier: it allows changes that semver considers compatible.","title":"Version specifiers"},{"category":"section","location":"compatibility/#Caret-specifiers","page":"Compatibility","text":"Example = \"1.2.3\" is equivalent to Example = \"^1.2.3\". A caret specifier allows everything up to the next change of the left-most non-zero digit:\n\nPkgA = \"^1.2.3\" # [1.2.3, 2.0.0)\nPkgB = \"^1.2\"   # [1.2.0, 2.0.0)\nPkgC = \"^1\"     # [1.0.0, 2.0.0)\nPkgD = \"^0.2.3\" # [0.2.3, 0.3.0)\nPkgE = \"^0.0.3\" # [0.0.3, 0.0.4)\nPkgF = \"^0.0\"   # [0.0.0, 0.1.0)\nPkgG = \"^0\"     # [0.0.0, 1.0.0)\n\nThe pre-1.0 rows follow from semver's convention that 0.x minor bumps (and 0.0.x patch bumps) may break things: 0.2.3 is not assumed compatible with 0.3.0, and 0.0.3 not with 0.0.4.","title":"Caret specifiers"},{"category":"section","location":"compatibility/#Tilde-specifiers","page":"Compatibility","text":"A tilde allows only patch-level changes (when at least a minor version is given):\n\nPkgA = \"~1.2.3\" # [1.2.3, 1.3.0)\nPkgB = \"~1.2\"   # [1.2.0, 1.3.0)\nPkgC = \"~1\"     # [1.0.0, 2.0.0)\nPkgD = \"~0.2.3\" # [0.2.3, 0.3.0)\nPkgE = \"~0.0.3\" # [0.0.3, 0.1.0)\nPkgF = \"~0.0\"   # [0.0.0, 0.1.0)\nPkgG = \"~0\"     # [0.0.0, 1.0.0)","title":"Tilde specifiers"},{"category":"section","location":"compatibility/#Equality-and-inequality-specifiers","page":"Compatibility","text":"An exact version is selected with =, and one-sided ranges with <, >= (or ≥):\n\nPkgA = \"=1.2.3\"   # exactly 1.2.3\nPkgB = \">= 1.2.3\" # [1.2.3, ∞)\nPkgC = \"< 1.2.3\"  # [0.0.0, 1.2.3)","title":"Equality and inequality specifiers"},{"category":"section","location":"compatibility/#Hyphen-ranges","page":"Compatibility","text":"A hyphen with spaces around it gives an inclusive range:\n\nPkgA = \"1.2.3 - 4.5.6\" # [1.2.3, 4.5.6]\n\nDigits left unspecified in the first endpoint count as zero; in the second endpoint they act as a wildcard:\n\nPkgA = \"1.2 - 4.5.6\" # [1.2.0, 4.5.6]\nPkgB = \"1 - 4.5.6\"   # [1.0.0, 4.5.6]\nPkgC = \"1.2.3 - 4.5\" # [1.2.3, 4.6.0)\nPkgD = \"1.2.3 - 4\"   # [1.2.3, 5.0.0)\nPkgE = \"0.2 - 0.5.6\" # [0.2.0, 0.5.6]\nPkgF = \"0.2 - 0\"     # [0.2.0, 1.0.0)","title":"Hyphen ranges"},{"category":"section","location":"compatibility/#Unions","page":"Compatibility","text":"Several specifiers separated by commas are allowed as a union:\n\nExample = \"1.2, 2\"     # [1.2.0, 3.0.0)\nOther   = \"0.2, 1\"     # [0.2.0, 0.3.0) ∪ [1.0.0, 2.0.0)\nExact   = \"=0.10.1, =0.10.3\"","title":"Unions"},{"category":"section","location":"compatibility/#compat-fixing-conflicts","page":"Compatibility","text":"Suppose your project depends on B and you want to add C, but B declares D = \"0.1\" while C needs D at 0.2 — the resolver reports an unsatisfiable requirement (see Interpreting and resolving version conflicts). If you believe B actually works fine with D 0.2, the constraint is just stale, and the fix is to widen it:\n\n(@v1.12) vpkg> dev B\n\nThen edit the [compat] entry in ~/.julia/dev/B/Project.toml:\n\n[compat]\nD = \"0.1, 0.2\"\n\nre-resolve and verify:\n\n(@v1.12) vpkg> up\n\n(@v1.12) vpkg> test B\n\nYour environment now uses the widened copy of B. Finish by contributing the compat change back to B's repository so a released version carries it — then you can free B and return to registered releases.","title":"Fixing conflicts"},{"category":"section","location":"getting-started/#Getting-Started","page":"Getting Started","text":"This is a quick tour of the most common operations: installing packages, using them, and keeping separate projects in separate environments. Everything here uses the Pkg REPL mode, so start Julia, load VibePkg, and press ]:\n\njulia> using VibePkg\n\njulia> ] # press ] at an empty prompt\n\n(@v1.12) vpkg>\n\nThe prompt shows the active environment — here the default v1.12 environment. To leave the Pkg REPL, press backspace on an empty line.\n\nEach REPL command also exists as an ordinary function, which is what you want in scripts: VibePkg.add(\"Example\") does the same as vpkg> add Example.","title":"Getting Started"},{"category":"section","location":"getting-started/#Installing-the-vpkg-command","page":"Getting Started","text":"VibePkg also declares a Julia app named vpkg. Install the repository with Julia's built-in Pkg app command:\n\n(@v1.12) pkg> app add https://github.com/KristofferC/VibePkg.jl\n\nFor a source checkout, use app develop /path/to/VibePkg.jl instead. Once ~/.julia/bin is on PATH, every REPL-mode command is available directly from the shell:\n\nvpkg status\nvpkg add Example\nvpkg up\nvpkg --help\n\nvpkg operates on the nearest project in the current directory or one of its parents. If there is no nearby project it uses Julia's versioned default environment. Set JULIA_PROJECT, or pass a Julia project option before the app separator (vpkg --project=/path/to/env -- status), to select one explicitly.\n\nnote: Note\nSome output in the examples below is abbreviated for readability.","title":"Installing the vpkg command"},{"category":"section","location":"getting-started/#Installing-and-using-a-package","page":"Getting Started","text":"To install a package, use add:\n\n(@v1.12) vpkg> add Example\n   Resolving package versions...\n   Installed Example ─ v0.5.5\n    Updating `~/.julia/environments/v1.12/Project.toml`\n  [7876af07] + Example v0.5.5\n    Updating `~/.julia/environments/v1.12/Manifest.toml`\n  [7876af07] + Example v0.5.5\n\nThe package is now available to load:\n\njulia> import Example\n\njulia> Example.hello(\"friend\")\n\"Hello, friend\"\n\nSeveral packages can be added in one command:\n\n(@v1.12) vpkg> add JSON StaticArrays\n\nstatus (or the short form st) shows what is in the environment:\n\n(@v1.12) vpkg> st\nStatus `~/.julia/environments/v1.12/Project.toml`\n  [7876af07] Example v0.5.5\n  [682c06a0] JSON v0.21.4\n  [90137ffa] StaticArrays v1.9.7\n\nThe [7876af07] prefix is the first part of the package's UUID, which identifies it uniquely even if two packages share a name.\n\nPackages are removed with rm and updated with up:\n\n(@v1.12) vpkg> rm JSON StaticArrays\n\n(@v1.12) vpkg> up\n\nIf a package in your environment is not at its newest version, status marks it with ⌃, and up will move it forward as far as compatibility constraints allow.","title":"Installing and using a package"},{"category":"section","location":"getting-started/#Getting-started-with-environments","page":"Getting Started","text":"So far everything went into the default environment. It is often better to give each project its own environment, so that projects don't have to agree on package versions. Create and switch to one with activate:\n\n(@v1.12) vpkg> activate tutorial\n  Activating new project at `~/tutorial`\n\n(tutorial) vpkg> st\nStatus `~/tutorial/Project.toml` (empty project)\n\nThe new environment is empty — it knows nothing about the packages added to the default environment. Add what the project needs:\n\n(tutorial) vpkg> add Example JSON\n\nThis creates two files in ~/tutorial: Project.toml, listing the direct dependencies, and Manifest.toml, recording the exact version of everything in the dependency graph. Together they make the environment reproducible — see Working with Environments.\n\nactivate with no argument brings you back to the default environment:\n\n(tutorial) vpkg> activate\n  Activating project at `~/.julia/environments/v1.12`\n\n(@v1.12) vpkg>\n\nnote: Note\nEven with many environments, each package version is downloaded and stored only once on disk, so environments are cheap. Prefer small per-project environments over putting everything in the default one.","title":"Getting started with environments"},{"category":"section","location":"getting-started/#Asking-for-help","page":"Getting Started","text":"Typing ? in the Pkg REPL lists all commands with a one-line description, and ? add (for example) shows the full help for a single command:\n\n(@v1.12) vpkg> ?\n\n(@v1.12) vpkg> ? add","title":"Asking for help"},{"category":"section","location":"workspaces/#Workspaces","page":"Workspaces","text":"A workspace groups several projects into one development environment. Every member keeps its own Project.toml, dependencies, and compatibility bounds, while the workspace has one shared resolution recorded in the root Manifest.toml.\n\nWorkspaces are most useful when the projects need to work together as one checkout: sibling packages in a monorepo, a package plus its tests and documentation, or several applications and libraries developed in lockstep. They are not merely a way to organize directories—the resolver considers the dependencies and compatibility requirements of every member together.","title":"Workspaces"},{"category":"section","location":"workspaces/#When-to-use-a-workspace","page":"Workspaces","text":"Use a workspace when:\n\nseveral packages in one repository should be developed and tested against one another's current source trees;\nCI should verify that all members have a mutually compatible dependency resolution;\ntests, documentation, examples, or benchmarks should use the same versions as the package they exercise; or\nyou want one reproducible development manifest for the whole repository.\n\nPrefer separate environments when:\n\nprojects intentionally need different versions of the same dependency or different Julia versions;\neach project must have an independently reproducible manifest;\nyou are testing several compatibility combinations, such as lower bounds and latest dependencies; or\nresolving every project together would couple otherwise unrelated work.\n\nA workspace gives each dependency one version in the shared manifest. This is its main benefit and its main constraint: incompatibilities between members are found immediately, but intentionally different resolutions require separate environments.","title":"When to use a workspace"},{"category":"section","location":"workspaces/#A-monorepo-workspace","page":"Workspaces","text":"Here is a repository with an umbrella root, two packages, and separate test and documentation projects:\n\nMyRepository/\n├── Project.toml\n├── Manifest.toml\n├── packages/\n│   ├── CorePkg/\n│   │   ├── Project.toml\n│   │   └── src/CorePkg.jl\n│   └── WebPkg/\n│       ├── Project.toml\n│       └── src/WebPkg.jl\n├── test/\n│   ├── Project.toml\n│   └── runtests.jl\n└── docs/\n    ├── Project.toml\n    └── make.jl\n\nThe root Project.toml declares paths relative to itself:\n\n[workspace]\nprojects = [\"packages/CorePkg\", \"packages/WebPkg\", \"test\", \"docs\"]\n\nThe root may be an ordinary package or an umbrella project containing only the [workspace] table. Each member is still a complete project. For example, if WebPkg imports CorePkg, its own Project.toml declares that dependency:\n\nname = \"WebPkg\"\nuuid = \"22222222-2222-2222-2222-222222222222\"\nversion = \"0.1.0\"\n\n[deps]\nCorePkg = \"11111111-1111-1111-1111-111111111111\"\n\nWorkspace membership makes CorePkg path-tracked from its local member directory, so a sibling dependency does not need a [sources] entry. Membership does not make every package visible to every other package: each member must declare every package it imports in its own [deps].\n\nimportant: Code loading remains project-local\nA workspace shares a manifest, not a dependency list or load path. At the REPL, using and import can directly load only packages declared in the currently active project's [deps]. A package being a dependency of some other workspace member—or merely appearing in the shared manifest—does not make it loadable from the active project.In the example above, using CorePkg works while WebPkg is active because WebPkg/Project.toml declares it. An umbrella root with no [deps] cannot directly load either member. Activate the project that owns the dependency, or add the dependency to the active member when it should be available there.","title":"A monorepo workspace"},{"category":"section","location":"workspaces/#The-shared-manifest","page":"Workspaces","text":"There is exactly one workspace manifest, next to the root Project.toml (or at the root's explicitly configured manifest path). Members do not get their own manifests. Resolution takes the union of member dependencies and intersects their compatibility requirements, then records the result in that root manifest.\n\nIf the repository commits a development manifest, commit the root manifest; member manifests are neither needed nor used as the workspace resolution. Repositories that do not normally commit manifests for libraries can make the same policy choice for the workspace root.\n\nYou may activate the root or any member:\n\n(@v1.12) vpkg> activate MyRepository/packages/WebPkg\n\n(WebPkg) vpkg> resolve\n\nAn operation started from a member edits that member's Project.toml, while resolution and installation update the root manifest. Activating a different member changes which project is considered active, not which manifest the workspace uses.","title":"The shared manifest"},{"category":"section","location":"workspaces/#Command-scope-and-workspace","page":"Workspaces","text":"Workspace resolution always accounts for all members. The --workspace flag does not enable that behavior; instead, on commands that offer it, the flag widens the command's visible or selected package set from the active member to the whole workspace.\n\nFor example:\n\nvpkg> status                # direct dependencies of the active member\nvpkg> status --workspace    # direct dependencies of every member\nvpkg> up --workspace        # update with every member's dependencies in scope\nvpkg> why --workspace JSON  # search paths starting from every member\n\ninstantiate, precompile, pin, and free also accept --workspace where their operation needs an all-members scope. See the REPL reference or API Reference for each command's exact options.","title":"Command scope and --workspace"},{"category":"section","location":"workspaces/#Nested-workspaces","page":"Workspaces","text":"A member may declare a [workspace] of its own. VibePkg discovers members recursively and merges the entire tree into the outermost root's manifest. Cycles and duplicate member paths are ignored safely, but a flat member list is usually easier for readers unless the repository itself has a meaningful nested structure.","title":"Nested workspaces"},{"category":"section","location":"workspaces/#Workspaces,-sources,-and-shared-environments","page":"Workspaces","text":"These related features solve different problems:\n\nA workspace combines several projects' dependency requirements into one resolution and automatically path-tracks its package members.\nA [sources] entry tells one project where to find a particular direct dependency. The dependency is resolved normally, but the source project is not added as a workspace member with its own top-level requirements.\nA shared environment such as @v1.12 or @tools is a named environment in the depot, useful for packages you want available across unrelated directories. It does not group repository projects together.\n\nFor the package-plus-tests pattern, including how test/Project.toml refers back to the package, see Adding tests. The [workspace] file format is summarized in Project.toml and Manifest.toml.","title":"Workspaces, sources, and shared environments"},{"category":"section","location":"registries/#Registries","page":"Registries","text":"A registry is an index of packages: for every registered package it records the name, UUID, repository location, released versions, their dependencies, and their compatibility constraints. When you add JSON, registries are what turn the name into a UUID and a set of resolvable versions. Any number of registries can be installed at once — including private ones next to the public General registry — and packages from all of them resolve together.","title":"Registries"},{"category":"section","location":"registries/#Managing-registries","page":"Registries","text":"On first use, when no registry is installed, the default registries (General) are installed automatically. Registries can also be managed explicitly with the registry REPL command; each subcommand exists as a function under VibePkg.Registry too (Registry.add, Registry.rm, Registry.update, Registry.status), taking the same strings.\n\nRegistries are always installed into the first depot in the depot stack — the same place all other writes go.","title":"Managing registries"},{"category":"section","location":"registries/#Adding","page":"Registries","text":"registry add with no arguments installs every registry the package server advertises — the same thing that happens automatically on first use. With arguments, each one may be:\n\na known registry name: registry add General works even without a package server (it falls back to a git clone of the known URL),\na git URL: registry add https://example.com/OurRegistry.git,\na local path: a git repository or a plain directory containing a Registry.toml.\n\n(@v1.12) vpkg> registry add https://github.com/JuliaRegistries/General\n\nWhen the configured package server advertises the requested registry, it is fetched from there (in the fast packed form); otherwise it is git-cloned.","title":"Adding"},{"category":"section","location":"registries/#Status","page":"Registries","text":"registry status (or registry st) lists the installed registries with their short UUID, source, and on-disk form, and — for registries the package server tracks — the serving server, the selected flavor, and whether an update is available:\n\n(@v1.12) vpkg> registry st\nRegistry Status\n  [23338594] General (https://github.com/JuliaRegistries/General.git)\n    packed registry, server https://pkg.julialang.org, flavor conservative","title":"Status"},{"category":"section","location":"registries/#Removing","page":"Registries","text":"registry remove (or registry rm) deletes an installed registry:\n\n(@v1.12) vpkg> registry rm General\n\nIf several installed registries share a name, disambiguate with the UUID, using the same name=uuid syntax as for packages:\n\n(@v1.12) vpkg> registry rm General=23338594-aafe-5451-b93e-139f81909106","title":"Removing"},{"category":"section","location":"registries/#Updating","page":"Registries","text":"registry update (or registry up) refreshes all installed registries; naming one or more updates just those:\n\n(@v1.12) vpkg> registry up General\n\nManual updates are rarely needed: registries are refreshed automatically once per session by the first add/develop, and on every up.","title":"Updating"},{"category":"section","location":"registries/#How-registries-are-stored","page":"Registries","text":"A registry can exist in the depot in four forms; registry status shows which one is installed:\n\npacked (the default with a package server): a single compressed tarball next to a small Name.toml pointing at it, read directly without unpacking. Fastest to download and read, and keeps file counts down.\nunpacked: the same server-provided data extracted to a directory tree (with a .tree_info.toml recording its content hash). Opt in with JULIA_PKG_UNPACK_REGISTRY=true — useful when you want to grep the files.\ngit clone: the result of adding a registry by URL or path when the package server does not provide it. Updates fetch and fast-forward the clone, so they appear as soon as they are committed upstream, at the cost of carrying the full git history on disk.\nbare directory: a plain copy of registry files, installed from a local non-git path. Never updated automatically.\n\nServer-backed registries (packed and unpacked) are updated by comparing the server's current content hash with the installed one and re-downloading on change. To convert a registry between forms, remove and re-add it — with the package server for the packed form, by URL for a git clone. Setting JULIA_PKG_SERVER=\"\" disables package servers entirely, forcing git clones even for the default registries.\n\nSince General is large, VibePkg nudges once per session if it finds it installed in the slower git/unpacked form; JULIA_PKG_GEN_REG_FMT_CHECK=false silences that.","title":"How registries are stored"},{"category":"section","location":"registries/#Registry-format","page":"Registries","text":"A registry is a directory (or tarball) with a top-level Registry.toml and one directory per registered package. Knowing the layout helps when reading a registry or maintaining a private one.","title":"Registry format"},{"category":"section","location":"registries/#Registry.toml","page":"Registries","text":"Identifies the registry and indexes its packages:\n\nname = \"OurRegistry\"\nuuid = \"...\"\nrepo = \"https://example.com/OurRegistry.git\"\ndescription = \"In-house packages\"\n\n[packages]\n7876af07-990d-54b4-ab0e-23690620f79a = { name = \"Example\", path = \"E/Example\" }\n\npath locates the package's directory, which holds the files below.","title":"Registry.toml"},{"category":"section","location":"registries/#Package.toml","page":"Registries","text":"The package's identity: name, uuid, the repo it is cloned from, and optionally subdir when the package lives in a subdirectory of its repository. An optional [metadata] table carries extensible per-package metadata; the table VibePkg itself understands is [metadata.deprecated]:\n\nname = \"Example\"\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nrepo = \"https://github.com/JuliaLang/Example.jl.git\"\n\n[metadata.deprecated]\nreason = \"no longer maintained\"\nalternative = \"BetterExample\"\n\nA deprecated package still resolves and installs normally, but is flagged in status output, omitted from tab completion, and status --deprecated prints the reason and alternative fields — see Deprecated packages below.","title":"Package.toml"},{"category":"section","location":"registries/#Versions.toml","page":"Registries","text":"One entry per released version, mapping it to the content hash of its source tree (which is what add verifies downloads against). A version can also be marked yanked:\n\n[\"0.5.4\"]\ngit-tree-sha1 = \"8eb7b4d4ca487caade9ba3e85932e28ce6d6e1f8\"\n\n[\"0.5.5\"]\ngit-tree-sha1 = \"46e44e869b4d90b96bd8ed1fdcf32244fddfb6cc\"\nyanked = true\n\nA yanked version is kept out of resolution for new environments but remains installable from existing manifests — see Yanked packages.","title":"Versions.toml"},{"category":"section","location":"registries/#Deps.toml-and-Compat.toml","page":"Registries","text":"Deps.toml records which dependencies each version has; Compat.toml records the constraints on them. To keep the files small, versions with identical data are grouped under dash ranges of versions:\n\n[\"0.8-0.9\"]\nDependencyA = \"0.4-0.5\"\n\n[\"0.9.2-0.9.5\"]\nDependencyB = \"1\"\n\nA dash range covers everything from the left endpoint up to where the right endpoint's prefix stops matching: 0.8-0.9 means [0.8.0, 0.10.0), 0.7-0 means [0.7.0, 1.0.0), and 0.7-* is unbounded above. Blocks can overlap — a version picks up the data of every block containing it, so in the example versions 0.9.2–0.9.5 depend on both DependencyA and DependencyB.\n\nWeakDeps.toml and WeakCompat.toml have the same shape and cover weak dependencies.","title":"Deps.toml and Compat.toml"},{"category":"section","location":"registries/#Registry-flavors","page":"Registries","text":"The default package server (pkg.julialang.org) offers two \"flavors\" of registry:\n\nconservative (the default): suitable for most users; every package and artifact in this flavor is available from the package server itself, with no need to download from other sources\neager: carries the very latest versions, even those the package server has not finished processing — some packages and artifacts may have to be downloaded from their original hosts (such as GitHub)\n\nThe environment variable JULIA_PKG_SERVER_REGISTRY_PREFERENCE selects the flavor; the selection itself happens on the server, per request. To switch, set it and update:\n\nENV[\"JULIA_PKG_SERVER_REGISTRY_PREFERENCE\"] = \"eager\"\n\nimport VibePkg\n\nVibePkg.Registry.update()\n\nVibePkg.Registry.status() shows the selected flavor next to the serving package server, along with whether a registry update is available.","title":"Registry flavors"},{"category":"section","location":"registries/#Deprecated-packages","page":"Registries","text":"A registry can mark a package as deprecated (no longer maintained, usually with a suggested alternative) via the [metadata.deprecated] table described above. Deprecated packages resolve normally, but status --deprecated flags them in your environment so you can plan a migration.\n\nRegistries can also yank individual versions — see Yanked packages.","title":"Deprecated packages"},{"category":"section","location":"registries/#Creating-and-maintaining-registries","page":"Registries","text":"VibePkg is a registry client: it installs, reads, and updates registries, but has no commands for authoring them. The ecosystem tools for that are Registrator.jl (register packages in General or a private registry), LocalRegistry.jl (create and update a registry with a git remote), and RegistryCI.jl (automated testing and merging for registry pull requests). A registry produced by any of them is added with registry add <url> as described above.","title":"Creating and maintaining registries"},{"category":"section","location":"devdocs/pkg_translation/#Coming-from-Pkg.jl","page":"Coming from Pkg.jl","text":"VibePkg is a from-scratch reimplementation of Pkg.jl with compatible user-visible behavior but differently organized internals. This page is a source map for developers who know Pkg.jl's code: where each Pkg module, type, and function lives in VibePkg. Read it next to Architecture, which describes VibePkg on its own terms.\n\n\"Pkg\" below means Pkg.jl's src/; unprefixed paths are VibePkg's.","title":"Coming from Pkg.jl"},{"category":"section","location":"devdocs/pkg_translation/#The-five-shifts-to-internalize","page":"Coming from Pkg.jl","text":"Most of the mapping falls out of five structural decisions:\n\nEnvCache became a value. Environments.Environment is an immutable snapshot. There is no original_project/original_manifest bookkeeping: operations load an Environment, compute a new one, and Environments.write_environment(old, new) diffs the two at write time. Undo entries are references to old snapshots.\nContext became Config plus explicit arguments. All process-state reads happen once per operation in Configs.Config, built by API.op_context() (which also loads/updates registries; the pair is API.OpContext(config, registries)). The environment is not inside the context — it is a separate load_environment value. ctx.julia_version survives only as a planner kwarg; there are no f(ctx::Context, ...) methods.\nOperations.jl split into pure planning and effectful execution. Each Operations.add-style body becomes: Planning.plan_* computes a target Environment with no network/disk access (materializing a git-tracked package is injected as a fetcher closure, Git.source_fetcher), then API.run_plan runs Execution.apply!, prints the diff, records undo, builds, and auto-precompiles. There is no plan struct: the planned Environment is the plan. Side operations get their own modules: BuildOps, TestOps, GCOps, AppsOps, ArtifactOps.\nPackageSpec no longer flows through the pipeline. VibePkg's API.PackageSpec is an immutable input record that never leaves the API layer: it is normalized into Planning.PackageRequest (registry request) or an already-materialized EnvFiles.RepoPackage (add-by-URL). Inside a plan the mutable working record is Planning.Node, private to the module and discarded after build_manifest.\nManifest entries are typed by how they are tracked. PackageEntry's nullable path/repo/version/tree_hash fields become EnvFiles.ManifestEntry with tracking::Union{PathTracked, RepoTracked, RegistryTracked}, plus predicates (is_path_tracked, ...) and accessors (entry_version, entry_tree_hash, entry_path, entry_repo_url, ...).\n\nA sixth, more diffuse one: Types.jl is dissolved into small modules included in strict layer order (see src/VibePkg.jl), importing each other explicitly.","title":"The five shifts to internalize"},{"category":"section","location":"devdocs/pkg_translation/#File-map","page":"Coming from Pkg.jl","text":"Pkg VibePkg Notes\nsrc/Pkg.jl src/VibePkg.jl same export/const-alias surface; adds the vpkg @main CLI; globals moved into API/Configs\nsrc/Types.jl dissolved PkgError → Errors; Project/Manifest types → EnvFiles; EnvCache/workspace/write → Environments; enums → Configs; stdlibs → Stdlibs; PackageSpec → API; name→UUID resolution → Planning; repo handling → Git\nsrc/project.jl, src/manifest.jl src/EnvFiles.jl readers keep Pkg's names; destructure → destructure_project/destructure_manifest\nsrc/Operations.jl src/Planning.jl + src/Execution.jl op semantics/resolution → Planning; download/install/instantiate → Execution; status/diff rendering → Display; build/test → BuildOps/TestOps\nsrc/API.jl src/API.jl same role, plus session state and run_plan; the gc body → GCOps\nsrc/Versions.jl src/Versions.jl same grammars and algorithms\nsrc/utils.jl src/Utils.jl except atomic_toml_write → Depots, safe_realpath → Environments, discover_repo → Git\n— src/Depots.jl new home for the depot schema: DepotStack, layout accessors, find_installed, log_usage (in Pkg scattered over Pkg.jl/Types.jl/Operations.jl)\n— src/Configs.jl Config plus the operation enums (PreserveLevel, UpgradeLevel, PackageMode)\nsrc/HistoricalStdlibs.jl folded into src/Stdlibs.jl same STDLIBS_BY_VERSION/UNREGISTERED_STDLIBS protocol, still populated externally\nsrc/GitTools.jl src/Git.jl + src/TreeHash.jl tree hashing is its own bottom-layer module\nsrc/PlatformEngines.jl src/Fetch.jl download/unpack/auth/package-server client\nsrc/Registry/ src/Registries.jl instance + lazy loading + queries + add/rm/update in one module; Pkg-namespace API in src/compat/Registry.jl\nsrc/Resolve/ src/Resolve/ vendored — see below\nsrc/Artifacts.jl src/ArtifactOps.jl + src/compat/Artifacts.jl mutating/network half vs. Pkg-namespace API\nsrc/Apps/Apps.jl src/AppsOps.jl + src/compat/Apps.jl \nsrc/REPLMode/ (3 files) src/REPLMode.jl declarations, parsing, help, and completions in one module\next/REPLExt/ (4 files) ext/REPLExt.jl + ext/precompile_workload.jl completions moved into REPLMode; no interactive compat editor\nsrc/generate.jl inline in src/API.jl \nsrc/precompile.jl src/precompile_workload.jl PrecompileTools-based, hermetic synthetic registry\nsrc/fuzzysorting.jl src/FuzzySorting.jl byte-identical file\nsrc/MiniProgressBars.jl src/MiniProgressBars.jl forked: adds ProgressLogger, drops print_progress_bottom\nsrc/BinaryPlatformsCompat.jl — no legacy platform shim; use Base.BinaryPlatforms\n— src/Timing.jl optional TimerOutputs instrumentation (Timing)\n— src/Queries.jl read-only façade for frontends/completions (Pkg's completion code reads EnvCache/registries directly)\n— src/Display.jl status/diff/compat rendering extracted from Operations.jl","title":"File map"},{"category":"section","location":"devdocs/pkg_translation/#Type-dictionary","page":"Coming from Pkg.jl","text":"Pkg VibePkg\nTypes.EnvCache Environments.Environment (immutable; no original_*; no pkg::PackageSpec — use project.name/uuid)\nTypes.Context Configs.Config + API.OpContext\nTypes.PackageSpec API.PackageSpec → Planning.PackageRequest / EnvFiles.RepoPackage / Planning.Node\nTypes.Project EnvFiles.Project (other → frozen raw, excluded from ==; sources typed as SourceSpec)\nTypes.Manifest EnvFiles.Manifest (project_hash typed field is authoritative; Pkg keeps the live value in manifest.other[\"project_hash\"])\nTypes.PackageEntry EnvFiles.ManifestEntry + tracking union\nTypes.GitRepo split into EnvFiles.SourceSpec (project [sources]), RepoTracked fields, RepoPackage\nTypes.ManifestRegistryEntry EnvFiles.RegistryRef\nTypes.Compat EnvFiles.Compat\nTypes.StdlibInfo Stdlibs.StdlibInfo\nRegistry.RegistryInstance/PkgEntry/PkgInfo/VersionInfo same names in Registries; the lazy payload is a separate LoadedRegistry behind accessors (registry_name(reg) instead of reg.name)\nRegistry.RegistrySpec — (management functions take strings; compat/Registry.jl has parse_registry_spec)\nAPI.UndoSnapshot entries of API.UNDO_STACKS — plain Environment references; capped at 50 on both sides\nREPLMode.CommandSpec/OptionSpec/ArgSpec REPLMode.CommandSpec (immutable; arg_kind/arg_count/opts fields)\nREPLMode.QString REPLMode.Word\nREPLMode.Command REPLMode.ParsedCommand","title":"Type dictionary"},{"category":"section","location":"devdocs/pkg_translation/#Function-map","page":"Coming from Pkg.jl","text":"Context, environments, env files:\n\nPkg VibePkg\nTypes.Context() API.op_context(; update_registry = :none/:auto/:force)\nTypes.EnvCache(env) Environments.load_environment(env)\nTypes.read_project / read_manifest same names in EnvFiles (pure cores: parse_project/parse_manifest)\nwrite_project / write_manifest / destructure EnvFiles.write_project / write_manifest / destructure_project+destructure_manifest\nTypes.write_env Environments.write_environment(old, new) — undo is not its job\nTypes.write_env_usage Depots.log_usage\nTypes.find_project_file Environments.find_project_file\nTypes.collect_workspace Environments.workspace_members\nTypes.workspace_resolve_hash Environments.resolve_hash\nOperations.is_manifest_current Environments.is_manifest_current (internal; takes an Environment)\nPkg.depots()/depots1() Depots.depots(stack)/depots1(stack) — snapshot, not live Base.DEPOT_PATH\nPkg.logdir/envdir + inline joinpath(depot, \"packages\") etc. Depots.logdir, environments_dir, packages_dir, clones_dir, registries_dir, artifacts_dir, scratchspaces_dir, bin_dir\nOperations.find_installed Depots.find_installed — returns (path, installed::Bool)\nPkg.pkg_server() / Pkg.devdir() Configs.pkg_server() / Config.devdir\nTypes.stdlibs/stdlib_infos/is_stdlib/stdlib_version/get_last_stdlibs same names in Stdlibs\nAPI.add_snapshot_to_undo API.record_undo! (called from run_plan)\n\nInput normalization and repo handling:\n\nPkg VibePkg\nAPI.handle_package_input! API.validate_specs + API.split_specs + API.to_request\nTypes.project_deps_resolve! → manifest_resolve! → registry_resolve! → stdlib_resolve! → ensure_resolved Planning.resolve_request — one pure function, same lookup order\nTypes.handle_repo_add! / handle_repos_add! Git.materialize_repo_package! — runs before planning, returns a RepoPackage; wrapped as the fetcher closure (Git.source_fetcher) for plan-time materialization\nTypes.handle_repo_develop! / handle_repos_develop! dev-path logic in API (dev_clone_target) + Planning.plan_develop\nTypes.get_object_or_branch Git.lookup_rev\nTypes.resolve_projectfile! inlined in Git.materialize_repo_package!\nTypes.add_repo_cache_path Git.repo_cache_path — different key (Base.hash(url) vs sha1(url)[1:16]), so URL clone caches are not shared\nproject.jl get_path_repo Planning.project_source / Environments.source_location\n\nResolution and planning:\n\nPkg VibePkg\nOperations.add/rm/up/pin/free/develop Planning.plan_add/plan_rm/plan_up/plan_pin/plan_free/plan_develop + API.run_plan\nOperations.can_skip_resolve_for_add (inline fast path) Planning.plan_promote\nOperations.resolve_versions! Planning.resolve_versions — returns nodes instead of mutating specs and env.manifest\ndeps_graph, collect_fixed!, collect_project, tiered_resolve, targeted_resolve, load_direct_deps, load_all_deps, load_manifest_deps, load_tree_hash!, prune_manifest, get_compat_str, dropbuild, source_path, is_package_downloaded same (or bang-less) names in Planning — near-line-for-line ports\nOperations.update_manifest! + record_project_hash Planning.build_manifest (stamps Environments.resolve_hash)\nOperations.up_load_versions! Planning.level_spec + API.refresh_repo_packages\nOperations.set_compat Planning.plan_compat_entry / plan_compat\nAPI.resolve (= up(level = UPLEVEL_FIXED, mode = PKGMODE_MANIFEST)) a dedicated planner, Planning.plan_resolve\nOperations.default_preserve Configs.default_preserve\n\nExecution:\n\nPkg VibePkg\nOperations.download_source Execution.ensure_sources_installed! → Fetch.ensure_package_installed!\nOperations.install_archive / install_git Fetch.install_archive / Git.install_tree_from_git!\nOperations.find_urls Execution.repo_urls_for + Fetch.package_archive_urls\nOperations.get_archive_url_for_version Fetch.github_archive_url\nOperations.download_artifacts / collect_artifacts Execution.ensure_artifacts! / ArtifactOps.collect_artifact_installs\nOperations.fixups_from_projectfile! Execution.fixups_from_projectfile — pure, returns a new Manifest\nOperations.show_update Display.print_env_diff(io, old, new)\nAPI.instantiate internals (Operations.is_instantiated, ...) Execution.instantiate! — never rewrites the manifest; missing/stale manifests handled in API.instantiate by delegating to up\nOperations.sandbox / sandbox_preserve / abspath! Execution.sandbox_manifest + Execution.sandbox_preferences — shared slice + preferences; there is no common resolving sandbox function\n\nAcquisition and registries:\n\nPkg VibePkg\nPlatformEngines.download / get_auth_header / get_metadata_headers / register_auth_error_handler Fetch.download / get_auth_token / pkg_server_headers / register_auth_error_handler\nPlatformEngines.unpack / get_extract_cmd same names in Fetch\nPlatformEngines.verify (sha256) ArtifactOps.verify_sha256\nPlatformEngines.download_verify_unpack and friends — composed inline at call sites\nGitTools.clone/fetch/ensure_clone/checkout_tree_to_path/normalize_url/setprotocol! same names in Git\nGitTools.tree_hash TreeHash.tree_hash (+ tree_hash_matches for the legacy symlink variant)\nRegistry.reachable_registries, registry_info, query_deps_for_version, query_compat_for_version_multi_registry!, uuids_from_name, isyanked, treehash, REGISTRY_CACHE same names in Registries — laziness and caching design identical\nRegistry.uncompress_registry Fetch.uncompress_registry\nRegistry.download_default_registries Registries.add_default_registries! (driven from op_context)\nRegistry.add/rm/update Registries.add_registry!/remove_registry!/update_registries! (+ the VibePkg.Registry shim)\nOperations.get_pkg_deprecation_info Registries.deprecation_info\n\nREPL and rendering:\n\nPkg VibePkg\nREPLMode/command_declarations.jl (declaration DSL) REPLMode.build_command_table() — imperative register! calls\nREPLMode.do_cmds(str) / do_cmd(::Command) REPLMode.do_cmd(::AbstractString) (and do_cmd(::Vector) for the CLI)\nargument_parsers.jl micro-syntax (parse_package, looks_like_url, extract_revision/extract_subdir/extract_version, GitHub URL unwrapping) same-named helpers in REPLMode (package_word_tokens + fold_package_tokens replace parse_package)\next/REPLExt/completions.jl REPLMode.completions_for, fed by Queries\nREPLExt.promptf / create_mode / repl_init REPLExt.promptf / create_mode / install_in (+ public REPLMode.install_repl!)\nOperations.print_status, status_compat_info, status_ext_info, print_compat, compat_line same names in Display\nOperations.print_diff / diff_array Display.print_diff_body / print_diff_rows\nOperations.git_head_env API.git_head_env\n@pkg_str, REPLMode.pkgstr, REPLMode.TEST_MODE same on both sides\n\nSide operations:\n\nPkg VibePkg\nOperations.build_versions BuildOps.build!\nOperations.dependency_order_uuids BuildOps.topo_order\nOperations.test TestOps.test!\nOperations.gen_target_project TestOps.sandbox_project\nOperations.gen_subprocess_flags / get_threads_spec TestOps.test_subprocess_flags / test_threads_spec\nAPI.gc body / Pkg._auto_gc GCOps.gc / API._auto_gc (throttled by logs/gc.stamp)\nApps.add/develop/rm/update/status AppsOps.app_add/app_develop/app_rm/app_update/app_status (+ the VibePkg.Apps shim)\nApps.generate_shims_for_apps/generate_shim/shell_shim/windows_shim AppsOps.write_shim/shim_contents/sh_shim/bat_shim\nPkgArtifacts.ensure_artifact_installed ArtifactOps.ensure_artifact_installed! (drop-in wrapper: VibePkg.Artifacts.ensure_artifact_installed)\nPkgArtifacts.bind_artifact!/unbind_artifact!/create_artifact/remove_artifact/verify_artifact same names in VibePkg.Artifacts (compat shim over ArtifactOps/TreeHash)\nPkg.generate (src/generate.jl) API.generate\n\nSession state:\n\nPkg VibePkg\nPkg.OFFLINE_MODE, UPDATED_REGISTRY_THIS_SESSION, IN_REPL_MODE, PREV_ENV_PATH same names, as API Refs/ScopedValues\nPkg.DEFAULT_IO Utils.DEFAULT_IO (ScopedValue on both sides)\nAPI.undo_entries / max_undo_limit API.UNDO_STACKS / API.MAX_UNDO\nPkg._autoprecompilation_enabled API.AUTO_PRECOMPILE_ENABLED\nPkg._auto_gc_enabled / DEPOT_ORPHANAGE_TIMESTAMPS API.AUTO_GC_ENABLED / the logs/gc.stamp file\nTypes.num_concurrent_downloads() Config.concurrency\nPkg.RESPECT_SYSIMAGE_VERSIONS Config.respect_sysimage_versions","title":"Function map"},{"category":"section","location":"devdocs/pkg_translation/#The-resolver","page":"Coming from Pkg.jl","text":"src/Resolve/ is vendored from Pkg. fieldvalues.jl and versionweights.jl are byte-identical; maxsum.jl is within a few lines. Resolve.jl carries minimal-diff alias shims (const Registry = Registries, const Types = Stdlibs) so the port stays close to upstream, plus @timeit instrumentation; graphtype.jl has localized performance rewrites (equivalence-class grouping, BitMatrix slicing, compat-mask reuse). Everything else — including ResolverTimeoutError and JULIA_PKG_RESOLVE_MAX_TIME — is as in Pkg.","title":"The resolver"},{"category":"section","location":"devdocs/pkg_translation/#Same-name,-same-place","page":"Coming from Pkg.jl","text":"Beyond the tables above, whole families kept their Pkg names and can be found by grepping for the identical identifier: the Versions API (semver_spec, VersionSpec, VersionRange, matches_spec_range!), the Registries query layer, the Git clone/fetch layer, the Utils helpers (printpkgstyle, can_fancyprint, stdout_f/stderr_f, set_readonly, mv_temp_dir_retries), the Stdlibs tables, the install mechanics (version slugs, *.pid pidlocks, temp-dir-plus-rename), the usage-log file names, and the pinned user-facing strings (VibePkg keeps each at a single call site, so grep-for-message works even better than in Pkg).","title":"Same name, same place"},{"category":"section","location":"devdocs/pkg_translation/#False-friends","page":"Coming from Pkg.jl","text":"Same name, different thing — worth knowing before grepping:\n\nName Pkg VibePkg\nPackageSpec mutable, flows through the whole operation immutable input record, never leaves API\nresolve alias for up(level = UPLEVEL_FIXED, mode = PKGMODE_MANIFEST) its own planner (plan_resolve)\nbuild recursive dep closure, instantiates, resolves a build sandbox exactly the requested uuids, no instantiate, no sandbox resolve\ndo_cmd takes a parsed Command; string entry is do_cmds is the string/vector entry point\n\"sandbox\" one shared resolving Operations.sandbox for build and test no such function; only slice + preferences shared\nfind_installed returns a (possibly hypothetical) path returns (path, installed::Bool)\ndepots() live Base.DEPOT_PATH immutable DepotStack snapshot\nfixups_from_projectfile[!] mutating, ctx-based pure, returns a new Manifest\nwrite_env vs write_environment diffs against hidden originals, records undo as a side effect explicit (old, new), undo separate\nproject_hash live value in manifest.other the typed Manifest field\nis_manifest_current public, takes a path internal, takes an Environment","title":"False friends"},{"category":"section","location":"devdocs/pkg_translation/#Life-of-add-Example,-side-by-side","page":"Coming from Pkg.jl","text":"Stage Pkg VibePkg\nParse REPLMode.do_cmds → generated API.add wrapper (default registries, undo snapshot, handle_package_input!) REPLMode.do_cmd → API.add (validate_specs, split_specs)\nContext Types.Context() + Operations.update_registries API.op_context(update_registry = :auto)\nEnvironment EnvCache() built inside Context Environments.load_environment\nName → UUID the *_resolve! passes + ensure_resolved Planning.resolve_request (inside the plan)\nFast path Operations.can_skip_resolve_for_add, inline in Operations.add Planning.plan_promote\nResolve Operations.tiered_resolve → resolve_versions! (mutates specs and ctx.env) Planning.plan_add → tiered_resolve → resolve_versions (returns nodes)\nManifest Operations.update_manifest! mutates ctx.env.manifest Planning.build_manifest returns the planned Environment\nExecute download_source → fixups_from_projectfile! → download_artifacts → write_env → show_update → build_versions → _auto_precompile API.run_plan: Execution.apply! (sources → artifacts → fixups → diff-aware write) → Display.print_env_diff → record_undo! → BuildOps.build! → _auto_precompile","title":"Life of add Example, side by side"},{"category":"section","location":"devdocs/pkg_translation/#No-counterpart","page":"Coming from Pkg.jl","text":"Only in Pkg: Context-first methods and Context!; julia_version/ platform kwargs on operations; PKGMODE_COMBINED; RegistrySpec and symlinked registry installs; try_prompt_pkg_add (the REPL auto-install hook); the interactive TerminalMenus compat editor; precompile monitor/stop/cancel; Apps.precompile; BinaryPlatformsCompat; Operations.ensure_manifest_registries!; legacy test/REQUIRE.\n\nOnly in VibePkg: Timing (Timing); Queries; the vpkg CLI (@main in src/VibePkg.jl) and the (env) vpkg> prompt; the fetcher injection; the compat/ Pkg-namespace shims; functional updates (with_project/with_manifest/with_entry); AppsOps.migrate_shims!; MiniProgressBars.ProgressLogger.","title":"No counterpart"},{"category":"section","location":"devdocs/pkg_translation/#Testing-the-package-itself","page":"Coming from Pkg.jl","text":"The two suites are set up on different principles:\n\nAspect Pkg VibePkg\nRunner test/runtests.jl includes a hardcoded file list sequentially in one process, inside sandbox modules that snapshot/restore DEPOT_PATH, LOAD_PATH, ENV, and the active project the Testosterone runner: test files are auto-discovered, every file is standalone-runnable, and files run in parallel worker processes scheduled by recorded durations\nNetwork real network: Utils.check_init_reg downloads the actual General registry once per run (pkg-server tarball, else GitHub clone), and tests add real packages (TEST_PKG = the registered Example) fully hermetic: test/local_pkg_server.jl serves the pkg-server protocol from generated fixtures — a synthetic General (real General/Example UUIDs, Example 0.5.0–0.5.5, tarballs + a tagged local git repo for add-by-URL), tree hashes computed from the generated trees. ensure!() also points http_proxy/https_proxy at a dead port so any stray internet request fails loudly\nDepot isolation Utils.isolate(fn) per testset: fresh temp depot with the cached General registry symlinked in, optional pre-warmed LOADED_DEPOT one per-run temp depot (VIBEPKG_TEST_DEPOT, shared with workers) plus julia's bundled depots — never ~/.julia; workers boot on a loose stack so VibePkg's deps/REPL load, then each file's prelude calls LocalPkgServer.isolate!() to tighten the whole process to the strict stack\nHelpers test/utils.jl (Utils): temp_pkg_dir, with_temp_env, copy_test_package (fixtures under test/test_packages/), git_init_package, add_this_pkg test/local_pkg_server.jl (server + isolation) and test/testhelpers.jl (e.g. make_test_registry), included behind @isdefined guards so files stay standalone\nOrganization roughly by frontend surface (new.jl, pkg.jl, api.jl, repl.jl, ...) by VibePkg module (test/planning.jl, test/execution.jl, test/registries.jl, ...) plus parity suites (test/parity_gaps.jl, test/pkg_issues.jl, test/doc_features.jl); test/resolve.jl, test/resolve_utils.jl, and test/NastyGenerator.jl are direct ports\nStatic checks Aqua Aqua + JET (test/jet.jl) + ExplicitImports (test/explicit_imports.jl)\n\nBoth suites use HistoricalStdlibVersions as a test dependency for cross-julia-version stdlib tables; VibePkg bridges its data into VibePkg.Stdlibs (test/historical_stdlib_version.jl) since HSV registers into Pkg.Types directly.\n\nRemaining user-visible behavior differences are tracked in test/TEST_PARITY_TODO.md — check there before assuming a divergence is a bug.","title":"Testing the package itself"},{"category":"section","location":"glossary/#Glossary","page":"Glossary","text":"Project: a source tree with a standard layout: src for the Julia code, docs for documentation, test for tests, and — the part VibePkg cares about — a project file and usually a manifest file. A project may be a package, an application, or just an environment.\n\nProject file: Project.toml (or JuliaProject.toml) at the root of a project: metadata (name, UUID, version, authors), the direct dependencies with their UUIDs, and compatibility constraints. Hand-editable. See Project and Manifest files.\n\nManifest file: Manifest.toml next to a project file: the exact resolved state of the full dependency graph — every package, its version or source, and its dependencies. Machine-generated. A project plus its manifest is reproducible with instantiate. A Manifest-v1.12.toml variant is preferred by Julia 1.12 over the plain name, letting one project carry resolutions for several Julia versions — see Versioned manifests.\n\nPackage: a project with a name, UUID, and version that provides a Julia module other projects can depend on and load. Packages are the reusable unit; they are usually registered so they can be added by name. (A package is not the same thing as a module: the module is the language-level namespace, the package is the installable source tree that carries it — conventionally one module of the same name.)\n\nApplication: a project that provides standalone functionality rather than a reusable module — something you run, not something you depend on. Applications that declare an [apps] table can be installed as command-line executables; see Apps. An application may choose global configuration for its dependencies because it owns the process. A reusable package should not do so: the application embedding it owns that policy, and sibling packages may need a different setting.\n\nEnvironment: what determines the meaning of using/import in a session: a project file's dependencies together with the manifest's exact resolution. Different environments can hold different — even incompatible — versions of the same packages. See Working with Environments. Julia's code loading also accepts implicit environments — a bare directory of packages, no project or manifest — but everything VibePkg manages is an explicit project-file environment.\n\nWorkspace: a group of projects declared by a root project's [workspace] table, resolved together into one shared manifest. See Workspaces.\n\nRegistry: an index mapping package names and UUIDs to repository locations, released versions, dependencies, and compat information. The public default is the General registry. See Registries.\n\nDepot: the per-user directory tree (by default ~/.julia) holding everything the package manager stores: installed package versions, artifacts, registries, clones, shared environments, logs, and app shims. See Depots.\n\nDepot path: the stack of depots in use, from JULIA_DEPOT_PATH / Base.DEPOT_PATH. All depots are searched when loading; the first depot is where new content is written. Later entries often hold read-only system-wide content.\n\nLoad path: the stack of environments (from JULIA_LOAD_PATH / Base.LOAD_PATH) visible to using/import in a session; the active environment is its first entry.\n\nInstantiate (materialize): install everything a manifest records, exactly, recreating the environment on any machine. This is what makes a committed project-plus-manifest pair reproducible.\n\nContent-addressed storage: each package version and artifact is stored in the depot once, at a location derived from its content hash, and every environment that resolves to it references that single canonical copy — disk usage does not grow with the number of environments.\n\nPinned package: a package fixed at a specific version; no operation moves it until it is freed.\n\nDeveloped package: a package loaded from a local source tree, whose current state is always used, bypassing versions and resolution for that package.","title":"Glossary"},{"category":"section","location":"api/#API-Reference","page":"API Reference","text":"Every REPL command is a thin layer over one function in this reference, so anything you can do interactively you can also do from a script. The functions live in the VibePkg module; the most common ones (add, rm, up, status, …) are also exported.","title":"API Reference"},{"category":"section","location":"api/#Calling-conventions","page":"API Reference","text":"Operations that produce log output accept an io keyword to redirect it, e.g. VibePkg.add(\"Example\"; io = devnull). Pure queries such as project() and dependencies(), and session toggles such as offline(), do not produce log output and therefore have no io keyword.\n\nThe package operations add, develop, rm, up, pin, and free uniformly accept these argument shapes:\n\nVibePkg.add(\"Example\")                             # a name\nVibePkg.add([\"Example\", \"JSON\"])                   # several names\nVibePkg.add(name = \"Example\", version = \"0.5\")     # keyword form\nVibePkg.add(PackageSpec(name = \"Example\", version = \"0.5\"))\nVibePkg.add([PackageSpec(name = \"Example\"), PackageSpec(name = \"JSON\")])\nVibePkg.add([(; name = \"Example\"), (; name = \"JSON\", version = \"0.21\")])  # named tuples\n\nNote that plain strings are names only — the REPL micro-syntax (\"Example@0.5\", \"Example#master\") is not parsed by the API; use the keyword/PackageSpec forms for versions, revisions, URLs, and paths. test and build also accept the wrapper forms when each spec supplies a name or UUID; their other package-spec fields are not used. status, why, and precompile accept names, vectors, and PackageSpecs as described in their entries below.","title":"Calling conventions"},{"category":"section","location":"api/#The-pkg\"...\"-string-macro","page":"API Reference","text":"pkg\"add Example\"\npkg\"status --manifest\"\n\npkg\"...\" runs VibePkg REPL-mode syntax from Julia code. It is convenient when translating an interactive command literally into a script; for code that constructs package names or options dynamically, prefer the ordinary functions.","title":"The pkg\"...\" string macro"},{"category":"section","location":"api/#Types","page":"API Reference","text":"","title":"Types"},{"category":"section","location":"api/#PackageSpec","page":"API Reference","text":"PackageSpec(; name, uuid, version, url, rev, path, subdir)\nPackageSpec(name)\nPackageSpec(name, uuid)\nPackageSpec(name, version)\n\nAn immutable description of a package for an operation. Only meaningful combinations are accepted: url and path are mutually exclusive, rev requires a name, URL, or path, and a version cannot be combined with URL/path/rev tracking.","title":"PackageSpec"},{"category":"section","location":"api/#PreserveLevel","page":"API Reference","text":"Passed as preserve to add and develop (and optionally up): PRESERVE_ALL_INSTALLED, PRESERVE_ALL, PRESERVE_DIRECT, PRESERVE_SEMVER, PRESERVE_NONE, PRESERVE_TIERED (default), PRESERVE_TIERED_INSTALLED. See Preserve tiers.","title":"PreserveLevel"},{"category":"section","location":"api/#UpgradeLevel","page":"API Reference","text":"Passed as level to up: UPLEVEL_MAJOR (default), UPLEVEL_MINOR, UPLEVEL_PATCH, and VibePkg.UPLEVEL_FIXED. The first three are exported; the fixed-level compatibility spelling must be qualified.","title":"UpgradeLevel"},{"category":"section","location":"api/#PackageMode","page":"API Reference","text":"Several operations accept mode = PKGMODE_PROJECT (or :project) and mode = PKGMODE_MANIFEST (or :manifest). Project mode acts on direct dependencies; manifest mode widens the operation to the full resolved graph.","title":"PackageMode"},{"category":"section","location":"api/#Package-operations","page":"API Reference","text":"","title":"Package operations"},{"category":"section","location":"api/#VibePkg.add","page":"API Reference","text":"add(\n    pkgs; preserve = PRESERVE_TIERED, target = :deps,\n    prefer_loaded_versions = false, io = stderr\n)\n\nAdd packages to the project and resolve, download, install, and precompile as needed. preserve controls how much the existing environment may move. target = :weakdeps or :extras records registered packages in that project table without resolving or installing them.\n\nWith prefer_loaded_versions = true, resolution prefers versions of packages already loaded in the Julia session when they are not yet in the manifest. This is the default for an interactive vpkg> add, but ordinary API calls default to false for reproducibility.","title":"VibePkg.add"},{"category":"section","location":"api/#VibePkg.develop-/-VibePkg.dev","page":"API Reference","text":"develop(pkgs; shared = true, preserve = PRESERVE_TIERED, io = stderr)\n\nTrack packages by source path. Registered names are cloned to the shared dev directory (shared = true) or the project's dev/ folder (shared = false); specs with path are used in place. Developed packages are never auto-precompiled. rev is not supported — use add for revision tracking.","title":"VibePkg.develop / VibePkg.dev"},{"category":"section","location":"api/#VibePkg.rm","page":"API Reference","text":"rm(pkgs; mode = :project, all_pkgs = false, io = stderr)\n\nRemove packages. mode = :project removes direct dependencies; mode = :manifest removes manifest packages together with all their dependents. all_pkgs = true removes everything in scope instead of naming packages.","title":"VibePkg.rm"},{"category":"section","location":"api/#VibePkg.up-/-VibePkg.update","page":"API Reference","text":"up(pkgs = ...; level = UPLEVEL_MAJOR, mode = :project, preserve = nothing,\n   workspace = false, io = stderr)\n\nUpdate packages (all of them when none are named) within level, after force-refreshing the registries. mode = :manifest seeds all manifest packages; workspace = true includes all workspace members' dependencies; preserve controls how the non-named packages are held (defaults to all when names are given).","title":"VibePkg.up / VibePkg.update"},{"category":"section","location":"api/#VibePkg.pin","page":"API Reference","text":"pin(pkgs; all_pkgs = false, workspace = false, io = stderr)\n\nPin packages at their current version, or at version when the spec carries one (pinning to a version requires the package to be registered).","title":"VibePkg.pin"},{"category":"section","location":"api/#VibePkg.free","page":"API Reference","text":"free(pkgs; all_pkgs = false, workspace = false, io = stderr)\n\nReturn pinned, developed, or repo-tracked packages to registry tracking.","title":"VibePkg.free"},{"category":"section","location":"api/#VibePkg.resolve","page":"API Reference","text":"resolve(; io = stderr)\n\nMake the manifest consistent with the project without moving installed versions, e.g. after editing project files or changing a developed dependency's dependencies.","title":"VibePkg.resolve"},{"category":"section","location":"api/#VibePkg.instantiate","page":"API Reference","text":"instantiate(;\n    manifest = nothing, verbose = false, workspace = false,\n    julia_version_strict = false, update_on_mismatch = false, io = stderr\n)\n\nInstall exactly what the manifest records. With no manifest (or manifest = false) resolves from the project first. update_on_mismatch falls back to up when project and manifest disagree instead of erroring; julia_version_strict escalates manifest Julia-version warnings to errors.","title":"VibePkg.instantiate"},{"category":"section","location":"api/#VibePkg.status","page":"API Reference","text":"status(\n    pkgs = PackageSpec[];\n    mode = :project, diff = false, outdated = false, deprecated = false,\n    compat = false, extensions = false, workspace = false, io = stdout\n)\n\nPrint the environment. Supplying names, UUID-bearing PackageSpecs, or a vector filters the listing to matching packages; in manifest mode, a match is shown with its dependencies. See status in the REPL reference for the meaning of each toggle.","title":"VibePkg.status"},{"category":"section","location":"api/#VibePkg.compat","page":"API Reference","text":"compat()                      # print the [compat] table\ncompat(pkg)                   # remove pkg's entry\ncompat(pkg, version)          # set pkg's entry\ncompat(; current = true)      # fill missing entries from resolved versions","title":"VibePkg.compat"},{"category":"section","location":"api/#VibePkg.activate","page":"API Reference","text":"activate()                    # the default environment\nactivate(path)                # the project at path\nactivate(name; shared = true) # a named shared environment\nactivate(; temp = true)       # a fresh temporary environment\nactivate(\"-\")                 # the previously active environment\nactivate(; prev = true)       # the same, in keyword form\n\nSet the environment subsequent operations act on. Activating never installs anything. If path names a dependency developed at a local path, that dependency's project is activated; otherwise the string is treated as a filesystem path. Repeatedly activating the previous environment toggles between the two most recent environments.","title":"VibePkg.activate"},{"category":"section","location":"api/#VibePkg.generate","page":"API Reference","text":"generate(path; io = stderr) -> Dict{String, UUID}\n\nCreate a package skeleton (Project.toml and src/Name.jl) at path; the package is named after the last path component.","title":"VibePkg.generate"},{"category":"section","location":"api/#VibePkg.why","page":"API Reference","text":"why(pkgs; workspace = false, io = stdout)\n\nPrint the dependency paths from the project's direct dependencies to one or more packages as a tree, with already-printed sub-trees shown as Name (*). Names, vectors, and PackageSpecs are accepted.","title":"VibePkg.why"},{"category":"section","location":"api/#VibePkg.build","page":"API Reference","text":"build(pkgs = ...; verbose = false, io = stderr)\n\nRun deps/build.jl of the given packages (default: all direct dependencies with one), dependencies first.","title":"VibePkg.build"},{"category":"section","location":"api/#VibePkg.test","page":"API Reference","text":"test(pkgs = ...; test_args = String[], julia_args = String[],\n     coverage = false, allow_reresolve = true,\n     force_latest_compatible_version = false,\n     allow_earlier_backwards_compatible_versions = true, io = stderr)\n\nRun package test suites in a sandbox (default: the active project). test_args are passed to the test script's ARGS; julia_args are extra command-line flags for the test process. Both accept either a vector of strings or a Cmd. coverage may be a Bool or a coverage-path string.\n\nThe sandbox first tries to retain exact versions from the active manifest. If that is impossible, allow_reresolve = true permits a fresh resolution; setting it to false is useful when CI must verify the checked-in manifest. force_latest_compatible_version = true raises test dependencies toward the latest versions allowed by compat. By default it may still use earlier backwards-compatible releases; set allow_earlier_backwards_compatible_versions = false to require the latest compatible release exactly.","title":"VibePkg.test"},{"category":"section","location":"api/#VibePkg.precompile","page":"API Reference","text":"precompile(\n    pkgs = String[]; strict = false, timing = false,\n    workspace = false, io = stderr\n)\nprecompile(f; kwargs...)  # defer automatic precompilation while f runs\n\nInstantiate, then precompile the environment. pkgs limits the run to named packages and their dependencies; strict makes every compile failure throw; timing reports per-package time; workspace includes all workspace members. The do-block form batches environment-changing operations and precompiles once after the block.","title":"VibePkg.precompile"},{"category":"section","location":"api/#VibePkg.gc","page":"API Reference","text":"gc(; verbose = false, force = false, io = stderr)\n\nDelete depot content not reachable from any environment that still exists: package versions, artifacts, repo clones, and scratchspaces. force = true sweeps every depot in the stack instead of only the first.","title":"VibePkg.gc"},{"category":"section","location":"api/#VibePkg.offline","page":"API Reference","text":"offline(b::Bool = true)\n\nToggle offline mode for the session: no registry updates, resolution restricted to installed versions.","title":"VibePkg.offline"},{"category":"section","location":"api/#VibePkg.undo-/-VibePkg.redo","page":"API Reference","text":"undo()\nredo()\n\nStep the active environment backwards/forwards through its snapshot history (up to 50 states per project, kept for the session).","title":"VibePkg.undo / VibePkg.redo"},{"category":"section","location":"api/#Introspection-and-configuration","page":"API Reference","text":"","title":"Introspection and configuration"},{"category":"section","location":"api/#VibePkg.dependencies","page":"API Reference","text":"dependencies() -> Dict{UUID, PackageInfo}\n\nReturn the full manifest graph keyed by UUID. Each PackageInfo reports the package's name, version, tree hash, source path, dependency map, whether it is direct or pinned, and whether it tracks a registry, repository, or local path. Because UUIDs are the keys, two packages with the same name remain distinct.","title":"VibePkg.dependencies"},{"category":"section","location":"api/#VibePkg.project","page":"API Reference","text":"project() -> ProjectInfo\n\nReturn the active project's name, UUID, version, project-file path, direct dependencies, and whether the project is a package.","title":"VibePkg.project"},{"category":"section","location":"api/#VibePkg.readonly","page":"API Reference","text":"readonly() -> Bool\nreadonly(on::Bool; io = stderr) -> Bool\n\nQuery or change the active project's readonly flag. The setter writes the project file and returns the previous value. While enabled, operations that would change the environment fail.","title":"VibePkg.readonly"},{"category":"section","location":"api/#VibePkg.setprotocol!","page":"API Reference","text":"setprotocol!(;\n    domain = \"github.com\", protocol = nothing,\n    user = protocol == \"ssh\" ? \"git\" : nothing\n)\n\nConfigure how git clone URLs for a domain are rewritten during this session. For example, protocol = \"ssh\", user = \"git\" turns GitHub clone URLs into SSH URLs; protocol = \"https\" forces HTTPS; protocol = nothing disables the rewrite. When user is omitted it defaults to \"git\" for SSH and to no user otherwise.","title":"VibePkg.setprotocol!"},{"category":"section","location":"api/#VibePkg.Registry","page":"API Reference","text":"Registry.add()          # install the default registries\nRegistry.add(spec...)   # install by name (\"General\"), URL, or local path\nRegistry.rm(spec...)    # remove; spec is a name, \"name=uuid\", or a uuid\nRegistry.rm(; name, uuid) # programmatic removal without string parsing\nRegistry.update()       # update all installed registries\nRegistry.update(names...) # update only the named ones\nRegistry.status()       # list installed registries (form, server, flavor)\n\nSee Registries.","title":"VibePkg.Registry"},{"category":"section","location":"api/#VibePkg.Artifacts","page":"API Reference","text":"Re-exports artifact_meta, artifact_hash, artifact_exists, artifact_path, find_artifacts_toml, and select_downloadable_artifacts from the Artifacts standard library, and provides:\n\nensure_artifact_installed(name, artifacts_toml; platform = HostPlatform()) -> path\nverify_artifact(hash::SHA1) -> Bool\nremove_artifact(hash::SHA1)\ncreate_artifact(f) -> SHA1                 # f fills a fresh directory\nbind_artifact!(artifacts_toml, name, hash; platform, download_info, lazy, force)\nunbind_artifact!(artifacts_toml, name; platform)\n\nSee Artifacts.","title":"VibePkg.Artifacts"},{"category":"section","location":"api/#VibePkg.Apps","page":"API Reference","text":"Apps.add(pkg)           # install a registered package's apps by name\nApps.develop(path)      # run apps directly from a source tree\nApps.rm(name)           # remove by package or app name\nApps.update([name])     # update all apps, or one package/app\nApps.status(names...)   # list installed apps, optionally filtered\n\nSee Apps.","title":"VibePkg.Apps"},{"category":"section","location":"api/#Package-server-authentication-hooks","page":"API Reference","text":"Fetch.register_auth_error_handler(urlscheme, f) -> deregister::Function\nFetch.deregister_auth_error_handler(urlscheme, f)\n\nRegister f(url, pkgserver, err) -> (handled, should_retry) to run when a token for an authenticated package server cannot be produced — for example to launch a login flow that writes a fresh auth.toml. See Authentication hooks in the protocol reference.","title":"Package-server authentication hooks"},{"category":"section","location":"managing-packages/#Managing-Packages","page":"Managing Packages","text":"This chapter goes through the package operations in depth: adding packages from registries, git repositories, and local paths; developing packages; removing, updating, and pinning; testing and building; and housekeeping such as garbage collection and offline mode.","title":"Managing Packages"},{"category":"section","location":"managing-packages/#Adding-packages","page":"Managing Packages","text":"","title":"Adding packages"},{"category":"section","location":"managing-packages/#Adding-registered-packages","page":"Managing Packages","text":"The normal way to get a package is by name, from a registry (usually the General registry):\n\n(@v1.12) vpkg> add JSON\n\nVibePkg resolves a version of JSON compatible with everything else in the environment, downloads it together with its dependencies, and records the result. The direct dependency lands in Project.toml; the full dependency graph, with exact versions, lands in Manifest.toml. status shows the former, status --manifest the latter:\n\n(@v1.12) vpkg> st\nStatus `~/.julia/environments/v1.12/Project.toml`\n  [682c06a0] JSON v0.21.4\n\n(@v1.12) vpkg> st -m\nStatus `~/.julia/environments/v1.12/Manifest.toml`\n  [682c06a0] JSON v0.21.4\n  [69de0a69] Parsers v2.7.2\n  ...\n\nAfter adding, the package can be loaded:\n\njulia> using JSON\n\njulia> JSON.json(Dict(\"foo\" => [1, \"bar\"])) |> print\n{\"foo\":[1,\"bar\"]}\n\nOnly direct dependencies in Project.toml are loadable by the project. A package such as Parsers may appear in status --manifest because JSON depends on it, but using Parsers is not allowed until you explicitly add Parsers. This keeps a project's imports honest: every package its code uses is recorded as one of its own dependencies.\n\nA specific version (or range — see Compatibility) can be requested with @:\n\n(@v1.12) vpkg> add JSON@0.21.1\n\nIf the requested version holds other packages back, status marks them: ⌃ means a newer version exists and could be installed, ⌅ means a newer version exists but compatibility constraints block it. status --outdated explains which constraints are responsible.\n\nIn REPL mode, add also prefers compatible versions that are already loaded in the Julia session but are not yet in the manifest. This avoids resolving an environment to a different version from the one currently in use. Ordinary API calls do not consult loaded modules by default; opt into the interactive behavior with prefer_loaded_versions = true.","title":"Adding registered packages"},{"category":"section","location":"managing-packages/#Adding-weak-and-extra-dependencies","page":"Managing Packages","text":"Package authors can add a registered package directly to [weakdeps] or the legacy [extras] table without resolving or installing it:\n\n(@v1.12) vpkg> add --weak ChainRulesCore\n(@v1.12) vpkg> add --extra Aqua\n\nThe API equivalents are VibePkg.add(\"ChainRulesCore\"; target = :weakdeps) and target = :extras. These targets accept registered packages only. A normal add of the same name later promotes it to [deps] and performs the usual resolution and installation.\n\nA registered package can also be tracked by a git revision instead of a released version:\n\n(@v1.12) vpkg> add Example#master\n(@v1.12) vpkg> add Example#025cf7e\n\nTracking a branch means up will pull in new commits to that branch. Tracking a commit gives you that exact state. To go back to following registered releases, use free:\n\n(@v1.12) vpkg> free Example","title":"Adding weak and extra dependencies"},{"category":"section","location":"managing-packages/#Adding-unregistered-packages","page":"Managing Packages","text":"A package that is not in a registry can be added by its repository URL:\n\n(@v1.12) vpkg> add https://github.com/fredrikekre/ImportMacros.jl\n\nThis clones the repository, reads the package's Project.toml for its name and UUID, and tracks the default branch (a #revision suffix picks a branch or commit). For SSH-style URLs, quote the word so the git@ part isn't misparsed:\n\n(@v1.12) vpkg> add \"git@github.com:fredrikekre/ImportMacros.jl.git\"\n\nIf the package lives in a subdirectory of the repository, append :subdir in the REPL or pass subdir to the API:\n\n(@v1.12) vpkg> add https://github.com/timholy/SnoopCompile.jl:SnoopCompileCore\n\njulia> VibePkg.add(url = \"https://github.com/timholy/SnoopCompile.jl\", subdir = \"SnoopCompileCore\")","title":"Adding unregistered packages"},{"category":"section","location":"managing-packages/#recursive-sources","page":"Managing Packages","text":"An unregistered package can depend on other unregistered packages without requiring a private registry. Declare each direct dependency normally and give its location in the package's [sources] table:\n\n[deps]\nPrivateCore = \"12345678-1234-1234-1234-123456789abc\"\n\n[sources]\nPrivateCore = {url = \"ssh://git@example.com/PrivateCore.jl.git\", rev = \"main\"}\n\nWhen the outer package is added by URL or local repository path, VibePkg follows these source declarations recursively. PrivateCore may in turn have its own [sources], allowing a complete private or unregistered dependency tree to resolve from one top-level add command. This is useful for private applications and related repositories that are not large enough to justify a registry. See the [sources] file-format reference for path and repository-subdirectory forms.","title":"Private dependency trees with [sources]"},{"category":"section","location":"managing-packages/#Adding-a-local-package","page":"Managing Packages","text":"add also accepts a local path to a git repository:\n\n(@v1.12) vpkg> add ~/code/MyPackage\n\nadd and develop do different things when given a local directory:\n\n add ./MyPackage develop ./MyPackage\nSource used the repository's committed state at add time the working tree at that path\nUncommitted edits ignored used\nLater edits or commits require running add again used the next time the package is loaded\nBest for recording a particular committed snapshot actively working on the package\n\nIn other words, add does not make the environment load code directly from the directory. It records the repository snapshot represented by its current commit. Use develop when you expect edits in that directory to affect the environment.\n\nwarning: Warning\nAdding a local directory is not the same as developing it. If you are editing the package, use develop ./MyPackage.","title":"Adding a local package"},{"category":"section","location":"managing-packages/#Developing-packages","page":"Managing Packages","text":"develop (short form dev) makes the environment load a package from a local source tree, whatever its state — edits take effect the next time the package is loaded, with no need to commit or re-add:\n\n(@v1.12) vpkg> dev Example\n\nFor a registered name like this, the repository is cloned to the development directory, by default ~/.julia/dev (override with JULIA_PKG_DEVDIR), and the environment tracks that path. With --local the clone goes into a dev/ folder next to the project instead, which is convenient for keeping everything in one place:\n\n(@v1.12) vpkg> dev --local Example\n\nA path develops that source directly, without any cloning:\n\n(@v1.12) vpkg> dev ~/code/MyPackage\n\nTo try this out: after dev Example, add a function to ~/.julia/dev/Example/src/Example.jl:\n\nplusone(x::Int) = x + 1\n\nrestart Julia (or use Revise.jl to avoid restarting), and:\n\njulia> Example.plusone(1)\n2\n\nwarning: Warning\nA package can only be loaded once per Julia session. If Example was already loaded, the developed copy is not picked up until Julia is restarted. Revise.jl removes most of this friction and is highly recommended when developing packages.\n\nfree stops tracking the path and returns the package to its registered version:\n\n(@v1.12) vpkg> free Example\n\nNote that develop tracks whatever is checked out at the path — it does not accept a #revision.\n\nVibePkg never edits, switches branches in, or pulls a developed source tree. When a clone already exists in the development directory it is reused; keeping that checkout current is your responsibility. Relative input paths are stored relative to the active project, while absolute paths stay absolute, so a project containing its developed dependencies can be moved as a unit when relative paths are used.\n\nThe trade-off is that a path-tracked environment is stateful: the manifest records the path, not the source tree's contents, and another user cannot reproduce it without a matching checkout at that path. If you add or remove a dependency in a developed package's Project.toml, run resolve so the active manifest learns about the changed dependency graph.","title":"Developing packages"},{"category":"section","location":"managing-packages/#Removing-packages","page":"Managing Packages","text":"rm removes packages from the project:\n\n(@v1.12) vpkg> rm JSON\n\nAnything that was only in the manifest to support a removed package disappears with it. rm --all empties the project.\n\nrm can also operate on the manifest directly, removing a package and everything that depends on it:\n\n(@v1.12) vpkg> rm --manifest Parsers","title":"Removing packages"},{"category":"section","location":"managing-packages/#Updating-packages","page":"Managing Packages","text":"up updates packages to their latest compatible versions, after refreshing the registries:\n\n(@v1.12) vpkg> up            # update everything\n(@v1.12) vpkg> up Example    # update only Example\n\nWhen packages are named, only they are candidates for updating; the rest of the environment is held back as much as possible (--preserve=all, the default when names are given). --preserve=direct also allows indirect dependencies to move, and --preserve=none lets everything move if it helps:\n\n(@v1.12) vpkg> up --preserve=none Example\n\nThe update can also be bounded by semver level: --major (the default), --minor, --patch, or --fixed (don't move this package at all — useful in combination with updating everything else).\n\nPackages tracking a git branch are updated to the latest commit on that branch; pinned and developed packages are never moved by up.","title":"Updating packages"},{"category":"section","location":"managing-packages/#inspecting-status","page":"Managing Packages","text":"status is also a diagnostic command. Its views answer different questions without changing the environment:\n\n(@v1.12) vpkg> status --manifest       # every transitive dependency\n(@v1.12) vpkg> status --outdated       # available updates and what blocks them\n(@v1.12) vpkg> status --diff           # changes since the environment in git HEAD\n(@v1.12) vpkg> status --compat         # declared compat entries\n(@v1.12) vpkg> status --extensions     # available extensions and their load state\n(@v1.12) vpkg> status --deprecated     # packages deprecated by their registry\n(@v1.12) vpkg> status --workspace      # direct dependencies of every workspace member\n\nPass one or more package names to focus a view, for example status --outdated JSON. In manifest mode, selecting a package also includes its direct dependencies. status --diff requires the environment files to be inside a git repository; it compares the working environment with the versions stored at HEAD.\n\nIn the ordinary listing, ⌃ means a newer version is available and may be installable, while ⌅ means constraints currently prevent installing the newest version. Use status --outdated PackageName to see the packages or compatibility bounds responsible.","title":"Inspecting an environment with status"},{"category":"section","location":"managing-packages/#Pinning-a-package","page":"Managing Packages","text":"A pin holds a package at its version through any operation until it is freed:\n\n(@v1.12) vpkg> pin Example\n    Updating `~/.julia/environments/v1.12/Project.toml`\n  [7876af07] ~ Example v0.5.5 ⇒ v0.5.5 ⚲\n\nPinned packages are marked with ⚲ in status. Pinning to a different version directly is also allowed, as is pinning everything:\n\n(@v1.12) vpkg> pin Example@0.5.1\n(@v1.12) vpkg> pin --all\n\nfree removes the pin:\n\n(@v1.12) vpkg> free Example","title":"Pinning a package"},{"category":"section","location":"managing-packages/#preserve-tiers","page":"Managing Packages","text":"add and develop need to fit new packages into the existing environment, and there is a trade-off between getting the newest versions and not disturbing what is already there. The --preserve option (preserve keyword in the API) picks the strategy:\n\nValue Meaning\ninstalled only use versions that are already installed in the depot\nall keep the version of every existing package (direct and indirect)\ndirect keep the version of every direct dependency\nsemver keep versions semver-compatible with what is currently there\nnone no constraint — resolve everything freshly\ntiered try all, then direct, then semver, then none (default)\ntiered_installed like tiered, but first try to add the package without downloading anything\n\nThe default, tiered, means an add first tries not to touch your existing versions at all and only relaxes if the resolver reports a conflict — so an add typically only ever adds. Setting the environment variable JULIA_PKG_PRESERVE_TIERED_INSTALLED=true makes tiered_installed the default, which additionally prefers fully offline-capable resolutions.","title":"Preserve tiers: how much may an operation change?"},{"category":"section","location":"managing-packages/#Testing-packages","page":"Managing Packages","text":"test runs a package's test suite (its test/runtests.jl) in a sandboxed environment assembled from the package's test dependencies:\n\n(@v1.12) vpkg> test Example\n     Testing Example\n     Testing Example tests passed\n\nWith no arguments the active project itself is tested. --coverage collects coverage statistics while the tests run. The API form accepts a few extra knobs: test_args (forwarded to the test script's ARGS) and julia_args (extra flags for the test process); either may be a string vector or a Cmd. By default a sandbox that cannot retain the exact manifest versions may re-resolve. CI jobs that intend to verify a checked-in manifest can forbid that fallback with allow_reresolve = false. The API Reference describes the latest-compatible-version testing options.","title":"Testing packages"},{"category":"section","location":"managing-packages/#Building-packages","page":"Managing Packages","text":"Packages with a deps/build.jl script have it run automatically the first time they are installed. build re-runs it on demand, for the named packages and their dependencies (dependencies first), or for the whole project:\n\n(@v1.12) vpkg> build IJulia\n    Building Conda ─→ `~/.julia/scratchspaces/44cfe95a-1eb2-52ea-b672-e2afdf69b78f/599391.../build.log`\n    Building IJulia → `~/.julia/scratchspaces/44cfe95a-1eb2-52ea-b672-e2afdf69b78f/6ac2b4.../build.log`\n\nBuild output is written to the indicated build.log; pass --verbose to see it directly.","title":"Building packages"},{"category":"section","location":"managing-packages/#Understanding-why-a-package-is-installed","page":"Managing Packages","text":"Manifests grow, and it is not always obvious what pulled a package in. why prints the dependency chains from your direct dependencies down to the package as a tree. Branches terminating in the queried package are marked with a (colored) ▶, and a package whose sub-tree has already been printed is shown as Name (*) instead of being expanded again:\n\n(@v1.12) vpkg> why Parsers\n  CSV\n  ├─ InlineStrings\n  │  └─▶ Parsers\n  ├─▶ Parsers\n  └─ WeakRefStrings\n     └─ InlineStrings (*)\n  JSON3\n  └─▶ Parsers","title":"Understanding why a package is installed"},{"category":"section","location":"managing-packages/#Interpreting-and-resolving-version-conflicts","page":"Managing Packages","text":"Sometimes there is no version assignment that satisfies everyone, and the resolver reports an error. Suppose the project requires packages B and C, B requires D at 0.1, and C requires D at 0.2:\n\n(@v1.12) vpkg> add C\nERROR: Unsatisfiable requirements detected for package D [6f418443]:\n D [6f418443] log:\n ├─possible versions are: [0.1.0, 0.2.0] or uninstalled\n ├─restricted by compatibility requirements with B [f4259836] to versions: 0.1.0\n │ └─B [f4259836] log:\n │   ├─possible versions are: 0.1.0 or uninstalled\n │   └─restricted to versions * by an explicit requirement, leaving only versions 0.1.0\n └─restricted by compatibility requirements with C [c99a7cb2] to versions: 0.2.0 — no versions left\n   └─C [c99a7cb2] log: ...\n\nRead the tree from the top: each restricted by line is one constraint on D, and the error is that their intersection is empty. Ways out, in rough order of preference:\n\nupdate the packages involved (up B C) — a newer release may have widened its compat;\nrelax your own [compat] entries if they are the restriction;\nif a dependency's compat is outdated, develop it, widen its [compat] entry, verify with its tests, and contribute the fix upstream (see Fixing conflicts);\nas a last resort, remove one of the conflicting packages.\n\nNote that if the environment already resolved once, a failed operation changes nothing — planning happens on an in-memory copy and is only written out when it succeeds.","title":"Interpreting and resolving version conflicts"},{"category":"section","location":"managing-packages/#Yanked-packages","page":"Managing Packages","text":"Registries can yank a released version — mark it as broken or harmful. Yanked versions are never selected by the resolver, but an environment that already has one keeps working; status flags it:\n\n(@v1.12) vpkg> st\nStatus `~/tutorial/Project.toml`\n  [7876af07] Example v1.2.0 [yanked]\n\nUpdate or re-resolve to move off a yanked version.","title":"Yanked packages"},{"category":"section","location":"managing-packages/#Garbage-collecting-old,-unused-packages","page":"Managing Packages","text":"Every package version, artifact, and repository clone is stored once in the depot and shared between environments. As environments change and disappear, content becomes unreachable. gc finds and deletes it:\n\n(@v1.12) vpkg> gc\n      Active manifest files: 5 found\n      Active artifact files: 32 found\n      Active scratchspaces: 6 found\n     Deleted 4 package installations (60.202 MiB)\n     Deleted 2 artifact installations (124.031 MiB)\n\nReachability is computed from usage logs the package manager keeps: every manifest that has been loaded, every artifact that has been used. A manifest file that no longer exists on disk no longer keeps its packages alive. Unreachable content is deleted immediately — there is no grace period. By default only the first depot in the depot stack is swept; gc --all (force = true in the API) sweeps all writable depots.\n\nA collection also runs automatically after up, pin, free, and rm when the depot has not been swept for a week. Set the environment variable JULIA_PKG_GC_AUTO=false (or call VibePkg.API.auto_gc(false) for the session) to disable the automatic collection.","title":"Garbage collecting old, unused packages"},{"category":"section","location":"managing-packages/#Offline-mode","page":"Managing Packages","text":"Offline mode makes VibePkg operate without touching the network: registries are not updated, and the resolver only considers package versions that are already installed.\n\njulia> VibePkg.offline(true)\n\nThe same is achieved by setting the environment variable JULIA_PKG_OFFLINE=true. Operations that fundamentally require the network (adding a package that is not installed, for instance) will fail cleanly.","title":"Offline mode"},{"category":"section","location":"managing-packages/#Undoing-changes","page":"Managing Packages","text":"Every operation that changes the environment first snapshots it, and undo steps back through those snapshots (redo steps forward again):\n\n(@v1.12) vpkg> rm JSON\n\n(@v1.12) vpkg> undo    # JSON is back\n\n(@v1.12) vpkg> redo    # removed again\n\nEach project keeps its own undo history (up to 50 states) for the duration of the session.","title":"Undoing changes"},{"category":"section","location":"managing-packages/#Package-servers","page":"Managing Packages","text":"By default packages, registries, and artifacts are downloaded from a package server (https://pkg.julialang.org), which serves them as content-verified tarballs — faster and firewall-friendlier than cloning repositories. Set JULIA_PKG_SERVER to use a different server (for instance a private mirror), or set it to the empty string to bypass package servers entirely and fetch from the original hosts. Content is verified by tree hash regardless of where it came from, and unregistered add url packages always come from their git host.\n\nDownloads run in parallel; JULIA_PKG_CONCURRENT_DOWNLOADS (default 8) controls how many at a time.\n\nThe wire protocol, server-side authentication, and how to hook into authentication failures are described in Package server protocols.","title":"Package servers"},{"category":"section","location":"managing-packages/#config-envvars","page":"Managing Packages","text":"Most of VibePkg's behavior can be configured through JULIA_PKG_* environment variables — the package server to use, offline mode, automatic precompilation and garbage collection, resolver budgets, and more. The full reference lives on its own page: Environment variables.","title":"Configuration through environment variables"},{"category":"section","location":"devdocs/architecture/#Architecture","page":"Architecture","text":"This page documents the internal architecture of VibePkg for developers working on the package itself. It describes the module layering, the core data types, and how data flows from user input through registry and environment parsing, into the resolver, and back out to disk and the terminal.","title":"Architecture"},{"category":"section","location":"devdocs/architecture/#Design-principles","page":"Architecture","text":"A few principles shape almost every module and are worth stating up front:\n\nValues, not caches. Project, Manifest, and Environment are immutable snapshots. An operation loads an Environment, computes a new Environment, and diffs the two at write time. There is no mutable EnvCache with \"original\" bookkeeping — an immutable snapshot is its own original, and an undo entry is just a reference to an old snapshot.\nConfiguration is data. Everything an operation needs from the process environment (depot stack, package server, offline flag, download concurrency, dev dir) is read once at the operation boundary into an immutable Configs.Config and passed down as an argument. Planning and Execution never read ENV.\nPure planning, effectful edges. Planning computes what the world should look like (a target Environment) without touching the network or disk; Execution makes disk match the target without making any resolution decisions. The one exception in Planning — materializing a package tracked by a git URL — is injected as a fetcher function so the module itself stays pure and testable.\nStrict layering. src/VibePkg.jl includes modules in a fixed order and each file may depend only on files included before it. Dependencies flow one way; there are no include-order cycles.\nRound-trip fidelity. Parsed TOML values keep the raw dict they came from (a raw field, excluded from ==/hash), and writers overlay typed fields onto a deep copy of that raw dict, so unknown keys in user files survive a read–modify–write cycle.","title":"Design principles"},{"category":"section","location":"devdocs/architecture/#Module-layering","page":"Architecture","text":"The include order in src/VibePkg.jl is the dependency order. Grouped by role:\n\n┌───────────────────────────────────────────────────────────────────┐\n│ Frontends        API, REPLMode (+ ext/REPLExt), compat/*          │\n├───────────────────────────────────────────────────────────────────┤\n│ Presentation     Display                                          │\n├───────────────────────────────────────────────────────────────────┤\n│ Side operations  GCOps, BuildOps, TestOps, AppsOps                │\n├───────────────────────────────────────────────────────────────────┤\n│ Core pipeline    Environments, Queries, Planning, Execution       │\n├───────────────────────────────────────────────────────────────────┤\n│ Resolution       Resolve (versionweights, fieldvalues,            │\n│                  graphtype, maxsum)                               │\n├───────────────────────────────────────────────────────────────────┤\n│ Data access      Registries, ArtifactOps                          │\n├───────────────────────────────────────────────────────────────────┤\n│ Acquisition      TreeHash, Git, Fetch                             │\n├───────────────────────────────────────────────────────────────────┤\n│ Foundations      Errors, Utils, MiniProgressBars, FuzzySorting,   │\n│                  Versions, EnvFiles, Depots, Configs, Stdlibs     │\n└───────────────────────────────────────────────────────────────────┘\n\nEach src/*.jl file defines a submodule of the same name. Modules import from each other explicitly (using ..Errors: pkgerror), never through globals.","title":"Module layering"},{"category":"section","location":"devdocs/architecture/#Foundations","page":"Architecture","text":"","title":"Foundations"},{"category":"section","location":"devdocs/architecture/#Errors","page":"Architecture","text":"Errors defines the single user-facing exception, PkgError, and the pkgerror(msg...) helper that throws it. It renders as its bare message. The convention is that every pinned error string is constructed at exactly one call site, which keeps error-message tests unambiguous.","title":"Errors"},{"category":"section","location":"devdocs/architecture/#Utils","page":"Architecture","text":"Shared low-level helpers, most importantly the IO indirection layer: all user output flows through stdout_f()/stderr_f(), which honor the DEFAULT_IO ScopedValue (used to silence the precompile workload) and wrap streams via unstableio into a single IOContext{IO} specialization. Also here: printpkgstyle (the styled/indented print primitive), TOML path normalization for Windows, set_readonly, and mv_temp_dir_retries — the atomic temp-dir-then-rename primitive with backoff that all content-addressed installs go through.","title":"Utils"},{"category":"section","location":"devdocs/architecture/#Versions","page":"Architecture","text":"Version bounds, ranges, and specs, plus the two version-string grammars:\n\nVersionBound — an NTuple{3,UInt32} with a significance count n, so \"1.2\" (two significant components) and \"1.2.0\" are distinct bounds.\nVersionRange — a lower/upper bound pair.\nVersionSpec — a frozen, normalized (sorted, merged, empties dropped) set of ranges. Specs are immutable after construction; copy is identity.\n\nThe semver_spec function parses the [compat] grammar (caret default, ~, =, inequalities, hyphen ranges, comma unions), which is deliberately disjoint from the VersionRange grammar used inside registries. matches_spec_range! is the batch membership test the resolver uses to build its compatibility bitmasks.","title":"Versions"},{"category":"section","location":"devdocs/architecture/#EnvFiles","page":"Architecture","text":"Project.toml and Manifest.toml as immutable values. The important types:\n\nProject — name/uuid/version, deps, weakdeps, extras, sources (SourceSpec: path or url+rev+subdir), compat (Compat keeps both the parsed VersionSpec and the original string), targets, workspace, apps, readonly, and the frozen raw dict.\nManifestEntry — name/uuid plus a tracking field of one of three concrete types: PathTracked (develop), RepoTracked (url+rev+tree hash), or RegistryTracked (version + tree hash + registry provenance). The AnyTracking union keeps dispatch type-stable, and predicates like is_registry_tracked plus accessors like entry_version dispatch on it.\nManifest — julia_version, manifest_format, project_hash, a Dict{UUID,ManifestEntry}, and (format 2.1+) a [registries] provenance table of RegistryRefs.\n\nReading is split into a pure core (parse_project / parse_manifest on dicts) and thin filesystem wrappers (read_project / read_manifest) that return empty values for missing files. Each TOML section has a typed happy-path reader plus a catch-all method that throws a pinned pkgerror on a wrong shape; dict access uses x::T unpack-asserts throughout. Manifest v1 files are converted to v2 form at read time, and conflicting path+repo fields are repaired at parse time in favor of path.\n\nWriting goes through destructure_project / destructure_manifest, which deep-copy the raw dict and overlay typed fields, deleting keys that equal their defaults. Because == skips raw, a freshly computed value compares equal to its re-parsed form, which is what makes diff-aware writes work. Functional update helpers (with_project, with_manifest, with_entry) replace mutation.","title":"EnvFiles"},{"category":"section","location":"devdocs/architecture/#Depots","page":"Architecture","text":"DepotStack is a snapshot of Base.DEPOT_PATH taken at operation start, so an operation sees one consistent stack. The rule everywhere is read from all depots, write to the first. Layout accessors (packages_dir, clones_dir, registries_dir, artifacts_dir, environments_dir, bin_dir, logdir, …) centralize the on-disk schema. find_installed probes both the current 5-char and legacy 4-char version slugs across the stack. log_usage appends compacted, pidlocked usage entries to logs/ — the marking data GC later relies on. atomic_toml_write gives torn-write-free TOML updates.","title":"Depots"},{"category":"section","location":"devdocs/architecture/#Configs","page":"Architecture","text":"The Config struct: depots, io, server (package server URL or nothing), offline, devdir, concurrency, respect_sysimage_versions. Its constructor is the one place environment variables (JULIA_PKG_SERVER, JULIA_PKG_OFFLINE, JULIA_PKG_DEVDIR, JULIA_PKG_CONCURRENT_DOWNLOADS, …) are consulted. Configs also owns the operation-option enums shared by all frontends: PreserveLevel, UpgradeLevel, and PackageMode.","title":"Configs"},{"category":"section","location":"devdocs/architecture/#Stdlibs","page":"Architecture","text":"The stdlib model distinguishes bundled unversioned stdlibs, externally versioned ones (jlls, Tar), and \"upgradable\" ex-stdlibs (DelimitedFiles, Statistics) that are treated as ordinary packages. stdlib_infos() lazily scans Sys.STDLIB. get_last_stdlibs(julia_version) resolves the stdlib set for a different Julia version from tables populated externally by HistoricalStdlibVersions.jl; julia_version === nothing means \"treat registered stdlibs as normal resolvable packages\".","title":"Stdlibs"},{"category":"section","location":"devdocs/architecture/#Acquisition-layer","page":"Architecture","text":"","title":"Acquisition layer"},{"category":"section","location":"devdocs/architecture/#TreeHash","page":"Architecture","text":"Pure git-exact tree hashing — the only content checksum used for packages and artifacts. tree_hash reproduces the git tree object hash (blob headers, git-style sort order, .git and empty dirs excluded), with a legacy symlink-size variant kept for old hashes; tree_hash_matches accepts either. Verification happens where content enters the store: after tarball extraction in Fetch.install_archive and ArtifactOps.try_install_from. Git checkouts trust LibGit2's canonical tree id instead of re-hashing.","title":"TreeHash"},{"category":"section","location":"devdocs/architecture/#Git","page":"Architecture","text":"LibGit2-backed (or CLI git when JULIA_PKG_USE_CLI_GIT is set) clone/fetch with progress, shallow-clone support, and rev-shape-aware fetch escalation (branch/tag refspecs first, unshallow for commit hashes, full refspecs as a fallback). Clone caches live at clones_dir/<sha1(url)[1:16]>. setprotocol! rewrites clone URLs per configured domain protocol/user.\n\nTwo functions matter to the pipeline: install_tree_from_git! — the fallback when no tarball source can produce an expected tree — and materialize_repo_package!, the effectful pre-phase of add-by-URL: clone or update the cache, resolve the rev, check the (sub)tree out into the package store, read its Project.toml, and return a RepoPackage (name, uuid, url, rev, subdir, tree hash, install path). source_fetcher(depots) packages this up as the closure Planning receives as its injected fetcher.","title":"Git"},{"category":"section","location":"devdocs/architecture/#Fetch","page":"Architecture","text":"The download and package-server client layer:\n\nURL priority. package_archive_urls returns candidate tarball URLs in order: the package server ($server/package/$uuid/$tree_hash), then a synthesized GitHub archive URL for GitHub-hosted repos.\nProtocol headers. Requests to server-matching URLs carry Julia-Pkg-Protocol: 1.0, Julia version/system triplet, CI/interactive hints, any JULIA_PKG_SERVER_* variables forwarded as Julia-* headers, and a bearer token when available.\nAuth. Tokens come from <depot1>/servers/<host>/auth.toml; expiring tokens are refreshed ahead of time via the recorded refresh_url, a 401 triggers one refresh-and-retry, and failures degrade to anonymous access. Error handlers are pluggable (register_auth_error_handler).\nInstall. ensure_package_installed! is content-addressed: if any depot already has the tree it is a no-op; otherwise, under a pidlock, each candidate URL is downloaded, extracted (zstd or gzip sniffed by magic bytes, piped through Tar.extract), tree-hash-verified, and atomically renamed into packages_dir. If all archive sources fail it falls back to Git.install_tree_from_git!.\nuncompress_registry reads a packed registry tarball into an in-memory filename => content dict without extracting to disk.","title":"Fetch"},{"category":"section","location":"devdocs/architecture/#Registries","page":"Architecture","text":"Registries is read-side only; add/update/remove of registries lives in the same file but calls down into Fetch/Git. Three on-disk forms coexist under each depot's registries/ directory: package-server packed tarballs (a Name.toml stub next to Name.tar.gz), unpacked directories (JULIA_PKG_UNPACK_REGISTRY), and git clones / plain directory copies.\n\nreachable_registries(depots) walks the depot stack (first depot wins conflicts) and returns RegistryInstances. Loading is lazy at two levels:\n\nRegistry index. A RegistryInstance holds only the path, tree info, and a lock until first access; registry_info-level access parses Registry.toml into a LoadedRegistry with a Dict{UUID,PkgEntry} and a name→UUIDs index. Packed registries are decompressed into an in-memory file dict rather than onto disk. A process-wide cache keyed on path and tree hash makes repeated loads cheap.\nPer-package info. A PkgEntry holds no version data until registry_info(reg, pkg) parses that package's Package.toml, Versions.toml, Deps.toml, Compat.toml (and weak variants) into a PkgInfo under double-checked locking, then evicts the raw files from the in-memory registry to free memory.\n\nCrucially, deps and compat stay range-compressed in memory exactly as the registry stores them — Dict{VersionRange,Set{UUID}} and Dict{VersionRange,Dict{UUID,VersionSpec}} — because uncompressing the whole registry per operation would be prohibitively slow. Query helpers (query_deps_for_version, query_compat_for_version_multi_registry!, uuids_from_name, is_version_yanked, deprecation_info, …) answer questions against the compressed form; the multi-registry compat query takes the first registry that actually has the version, per dependency.\n\nRegistry installation from a package server compares tree hashes advertised by $server/registries against what is installed and re-downloads on mismatch; git-cloned registries update via fetch + fast-forward merge.","title":"Registries"},{"category":"section","location":"devdocs/architecture/#The-resolver","page":"Architecture","text":"Resolve is a self-contained constraint solver, a port of Pkg's max-sum resolver. Its inputs are UUID-keyed and still registry-compressed; the Graph constructor is what converts them into numeric form:\n\nGraphData assigns each package an integer index, each version of a package a state index (plus one extra \"uninstalled\" state), and keeps the UUID↔index and version↔state mappings.\nFor every dependency edge it builds a BitMatrix compatibility mask over the two packages' state spaces, computed from the compressed compat data via Versions.matches_spec_range!. Requirements become per-package BitVector constraints. Fixed packages (the project itself, developed and pinned packages) constrain the graph but are excluded from the output.\n\nSolving proceeds in stages inside resolve(graph):\n\nsimplify_graph! propagates constraints and prunes: versions that can never be selected are dropped and equivalence classes of indistinguishable versions are collapsed.\nA greedy fast path tries to pick the highest version of everything and bails as soon as a nontrivial conflict appears — most real resolves finish here.\nOtherwise max-sum belief propagation (message passing over the factor graph, with decimation and snapshot-based backtracking) searches for an assignment. Version preference is encoded in FieldValue, a 5-level lexicographic objective: hard constraint violations dominate, then higher versions of explicitly required packages, then higher versions of everything else, then uninstalling unneeded packages. Already-loaded versions can be soft-preferred via a large VersionWeight bonus (used by the REPL so that add in a running session favors loaded versions).\nenforce_optimality! post-processes the solution to a local optimum and the solver re-runs with the previous solution as a lower bound until it stops improving.\n\nFailures throw ResolverError carrying a human-readable trace assembled from ResolveLog, a per-package journal of every pruning and propagation decision (this is what produces the familiar \"Unsatisfiable requirements detected\" message). A time limit (JULIA_PKG_RESOLVE_MAX_TIME) produces ResolverTimeoutError. The output of a successful resolve is simply Dict{UUID,VersionNumber} for the packages that should be installed.","title":"The resolver"},{"category":"section","location":"devdocs/architecture/#Environments","page":"Architecture","text":"Environments.Environment is the value the whole pipeline pivots on:\n\nstruct Environment\n    project_file::String\n    manifest_file::String\n    project::Project\n    manifest::Manifest\n    workspace::Vector{Pair{String, Project}}  # other workspace members\nend\n\nload_environment resolves the target (active project, @shared name, or path), reads project and manifest via EnvFiles, discovers workspace members (walking workspace.projects recursively and cycle-safely), and logs the manifest to manifest_usage.toml for GC liveness.\n\nwrite_environment(old, new) is diff-aware: the project file is written only if new.project != old.project and likewise for the manifest (remember == ignores raw). Before writing it re-derives the project [sources] table from the manifest and enforces the readonly project flag.\n\nresolve_hash(env) computes a SHA1 over the project's deps/weakdeps/compat; it is stored in the manifest as project_hash and is how is_manifest_current / Pkg.instantiate detect that the project changed since the manifest was resolved.\n\nQueries is a small read-only façade over environments, registries and stdlibs (registered package names, current dependency names, deprecation checks) used by frontends — REPL completions consume these instead of reaching into internals.","title":"Environments"},{"category":"section","location":"devdocs/architecture/#Planning","page":"Architecture","text":"Planning holds the operation semantics: every planner takes (Environment, Vector{RegistryInstance}, Config, ...) and returns a new Environment — there is no separate \"plan\" struct; the planned environment is the plan, and Execution later diffs it against the old one.","title":"Planning"},{"category":"section","location":"devdocs/architecture/#Normalizing-user-input","page":"Architecture","text":"The API layer converts user PackageSpecs into Planning.PackageRequest (name/uuid/version) or, for URL/path additions, into materialized RepoPackages. resolve_request performs name→UUID resolution in a fixed order: project deps/extras/weakdeps → manifest → registries → stdlibs, erroring on ambiguity and producing fuzzy \"did you mean\" suggestions (via FuzzySorting) on failure.","title":"Normalizing user input"},{"category":"section","location":"devdocs/architecture/#Building-resolver-input","page":"Architecture","text":"Inside a plan, manifest entries and requests become mutable working Node records (never escaping the module). Seed loaders assign each node a version constraint and a preserve policy: direct dependencies default to PRESERVE_DIRECT, manifest entries to PRESERVE_ALL, and project [sources] are overlaid on top. collect_fixed walks the project, workspace members, and the recursive closure of developed/repo-tracked packages — reading their actual Project.tomls on disk — and turns them into Resolve.Fixed entries (this is where the injected fetcher materializes repo-tracked trees that are not yet installed).\n\ndeps_graph then does a BFS from the required and fixed UUIDs, pulling per-registry compressed deps/compat/version data through Registries.registry_info, filtering yanked versions, and synthesizing single-version entries for stdlibs from Stdlibs.get_last_stdlibs (stdlibs are never resolved from registries; when resolving for the running Julia they are fixed, and \"upgradable\" ex-stdlibs are ordinary packages). Its output tuple is exactly what the Resolve.Graph constructor consumes.","title":"Building resolver input"},{"category":"section","location":"devdocs/architecture/#Tiered-resolution","page":"Architecture","text":"resolve_versions orchestrates: build requirements from nodes → deps_graph → Resolve.Graph → simplify_graph! → Resolve.resolve → apply the returned Dict{UUID,VersionNumber} back onto the nodes and compute the final per-package dependency maps via query_deps_for_version. JLL build-number quirks are fixed up around the solver, and tree hashes are attached from registry data.\n\nWhen the caller asks for tiered preservation (the default), tiered_resolve retries with progressively weaker constraints, catching ResolverError at each tier:\n\nPRESERVE_ALL_INSTALLED → PRESERVE_ALL → PRESERVE_DIRECT → PRESERVE_SEMVER → PRESERVE_NONE","title":"Tiered resolution"},{"category":"section","location":"devdocs/architecture/#Producing-the-target-environment","page":"Architecture","text":"build_manifest converts the resolved nodes into ManifestEntry values, choosing the tracking type per node (path / repo / registry), carrying weakdeps/extensions/apps from old entries (Execution corrects them after install from the real Project.tomls), recording registry provenance, pruning unreachable entries, and stamping project_hash. The result, packaged with the (possibly modified) project, is the planned Environment.\n\nPlanner entry points map one-to-one to operations: plan_add (with a plan_promote fast path that skips resolution when the request is already satisfiable from the manifest), plan_rm, plan_up, plan_develop, plan_pin, plan_free, plan_resolve, plan_compat. instantiate has no planner at all — the manifest is used as-is.","title":"Producing the target environment"},{"category":"section","location":"devdocs/architecture/#Execution","page":"Architecture","text":"Execution.apply!(old_env, planned_env, registries, config) makes disk match the plan:\n\nensure_sources_installed! walks the planned manifest and installs every tree that is missing from all depots. Downloads run concurrently under a Base.Semaphore(config.concurrency) with an aggregate progress bar; each install is pidlocked and atomic. Registry-tracked entries get candidate URLs from registry metadata (package server first), repo-tracked entries use their recorded URL, path-tracked entries are only checked for existence.\nensure_artifacts! collects each installed package's (Julia)Artifacts.toml selections and installs missing artifact trees, deduplicated by tree hash, with the same concurrency scheme.\nfixups_from_projectfile re-reads the installed packages' Project.tomls to correct manifest fields the registry cannot know (weakdeps, extensions, entryfile).\nwrite_environment(old_env, env) performs the diff-aware write.\n\ninstantiate! is the no-replan variant: it validates that every direct dep is present in the manifest, warns if project_hash is stale, and runs the same two install phases without ever rewriting the manifest.\n\nsandbox_manifest — slicing a manifest to a set of roots plus their recursive strong deps, absolutizing paths — lives here and is shared by the build and test sandboxes.","title":"Execution"},{"category":"section","location":"devdocs/architecture/#Artifacts","page":"Architecture","text":"ArtifactOps owns networked/mutating artifact work (the Artifacts stdlib owns lookup). Selection honors .pkg/select_artifacts.jl hooks (run in a minimal Julia subprocess) over the static platform-based selection, and Overrides.toml redirections are respected across the depot stack. Installation tries $server/artifact/$hash first, then the Artifacts.toml download entries (sha256-verified, then tree-hash-verified), through the same download → unpack → verify → atomic-rename path as packages. Every Artifacts.toml consulted is logged to artifact_usage.toml for GC.","title":"Artifacts"},{"category":"section","location":"devdocs/architecture/#Frontends-and-user-input","page":"Architecture","text":"","title":"Frontends and user input"},{"category":"section","location":"devdocs/architecture/#API","page":"Architecture","text":"API is the public façade. User input arrives as PackageSpecs — an immutable record of name/uuid/version/url/rev/path/subdir — validated by validate_specs (legal field combinations per operation) and partitioned by split_specs into registry requests vs repo-like additions.\n\nNearly every operation starts with op_context(), which builds the OpContext: a fresh Config (the single ENV read) plus reachable_registries — bootstrapping default registries into a fresh depot, and updating registries per policy (:none / :auto once per session / :force for up), all skipped when offline.\n\nThe standard mutating-operation shape is:\n\nload_environment → Planning.plan_* → run_plan\n\nwhere run_plan executes Execution.apply!, prints the diff (Display.print_env_diff), records an undo snapshot, runs deps/build.jl for newly installed packages (BuildOps.build!), and auto-precompiles. Precompilation itself is owned by Base (Base.Precompilation.precompilepkgs); VibePkg only decides when to call it. Operations that can strand data (rm, up, pin, free) end with _auto_gc, which runs a GC sweep if the last one is more than a week old.\n\nSession-level state lives only here, in module Refs and ScopedValues: offline mode, auto-precompile/auto-gc toggles, the once-per-session registry-update guard, IN_REPL_MODE (which makes add soft-prefer already-loaded versions), and the undo/redo stacks — per-project vectors of Environment references, capped at 50.","title":"API"},{"category":"section","location":"devdocs/architecture/#REPLMode-and-the-REPL-extension","page":"Architecture","text":"REPLMode implements the pkg> command language as data: a lazily built table of CommandSpecs, each mapping one command name (plus option specs and an argument shape) to exactly one API function. Input is tokenized (quote-aware, no escapes, with add A, B comma sugar), package words are parsed by a micro-syntax layer (Name@version, #rev, :subdir, URL/path detection, GitHub tree/commit URL unwrapping) and folded into PackageSpecs, and ParsedCommand dispatch calls the API function with converted options. Completions are served from Queries. A TEST_MODE flag makes do_cmd return the parsed (api, args, opts) tuple instead of executing, which is how the command language is unit-tested.\n\next/REPLExt.jl is a package extension that installs the interactive mode: a sticky prompt (with cached (env) vpkg> rendering), a completion provider, and an on_done that funnels the buffer into REPLMode.do_cmd. The same do_cmd(args::Vector) entry serves the vpkg CLI app installed via pkg> app add VibePkg (see the @main in src/VibePkg.jl).","title":"REPLMode and the REPL extension"},{"category":"section","location":"devdocs/architecture/#Display","page":"Architecture","text":"Display owns all user-visible rendering, byte-compatible with Pkg's pinned strings. Its two main consumers of pipeline data are print_env_diff — which takes two Environments and prints the +/-/↑/↓/~ lines for project and manifest — and print_status, which renders Pkg.status including upgrade-hold glyphs (⌃/⌅, computed against registry version info), not-downloaded markers, yanked/deprecated/loaded annotations, and the --outdated/--compat detail views.","title":"Display"},{"category":"section","location":"devdocs/architecture/#Compatibility-shims","page":"Architecture","text":"compat/ provides Pkg-namespace-compatible modules: Artifacts (stdlib re-exports plus install/bind/unbind bottoming out in ArtifactOps), Registry (add/rm/update/status over Registries), and Apps (thin wrappers over AppsOps). Top-level const add = API.add aliases in VibePkg.jl mirror Pkg's export surface.","title":"Compatibility shims"},{"category":"section","location":"devdocs/architecture/#Side-operations","page":"Architecture","text":"BuildOps runs deps/build.jl scripts deps-first in an isolated sandbox: a temp project containing only the package being built over a sandbox_manifest slice, executed in a julia -O0 subprocess with a clean load path. Preferences are flattened into the sandbox (see TestOps). Logs go next to dev packages or into a scratchspace keyed by tree hash, and each build logs scratch_usage.toml.\nTestOps constructs the test sandbox: a project from test/Project.toml (or synthesized from legacy [targets]), the tested package force-added and pinned as path-tracked, preferences flattened from the full load-path cascade (test project over parent environment over default environments, via Base.get_preferences) into the sandbox's JuliaLocalPreferences.toml, and a manifest merged from the parent environment (parent wins) — then resolved and installed through the normal plan_resolve/apply! pipeline before spawning the test subprocess with mirrored Base.JLOptions flags. Workspace members skip the sandbox and test in place.\nAppsOps manages per-app environments under <depot>/environments/apps/<Name> plus a global AppManifest.toml, and installs self-locating shell/batch shims into <depot>/bin that exec julia -m Module. Shims embed a version stamp and are migrated in place when the shim format changes. App installation stages the environment in a temp dir via the normal plan/apply pipeline, then swaps it in atomically.\nGCOps is mark-and-sweep with no grace period. Mark: compact the three usage logs (manifest_usage.toml, artifact_usage.toml, scratch_usage.toml), drop entries whose files no longer exist, then read every live manifest to collect reachable package trees, clones, artifact trees, and scratchspaces. Sweep: delete anything in those directories not in the keep-sets (first depot only, unless force). A gc.stamp file throttles the automatic weekly sweep.","title":"Side operations"},{"category":"section","location":"devdocs/architecture/#Life-of-a-pkg-add-Example","page":"Architecture","text":"Putting it all together:\n\nParse. REPLExt's on_done hands the buffer to REPLMode.do_cmd; tokenization and the package micro-syntax produce PackageSpec(\"Example\"), and the command table dispatches to API.add under IN_REPL_MODE.\nContext. op_context builds a Config (ENV read once: depot stack, server, offline, concurrency) and loads reachable_registries, updating them once per session unless offline.\nEnvironment. load_environment parses Project.toml/Manifest.toml into an immutable Environment (logging manifest usage for GC).\nNormalize. validate_specs/split_specs classify the request; resolve_request maps the name to a UUID via project → manifest → registries → stdlibs.\nPlan. plan_promote first checks whether the manifest already satisfies the request (fast path: just promote to a direct dep). Otherwise plan_add seeds nodes from the manifest with preserve levels, collects fixed packages, and deps_graph BFS-loads compressed registry data.\nResolve. Resolve.Graph turns UUIDs into indices and compat into bitmasks; greedy solve, falling back to max-sum with tiered preserve retries. Output: Dict{UUID,VersionNumber}.\nTarget. build_manifest produces the planned Environment with new ManifestEntrys, tree hashes, and registry provenance.\nExecute. Execution.apply! downloads missing package trees (pkg server → GitHub archive → git fallback; tree-hash-verified; atomic renames; concurrent under a semaphore), installs artifacts, fixes up weakdeps/extensions from real Project.tomls, and writes only the files whose values actually changed.\nReport. run_plan prints the env diff, pushes an undo snapshot, runs build scripts for new installs, and auto-precompiles via Base.Precompilation.precompilepkgs.","title":"Life of a pkg> add Example"},{"category":"section","location":"devdocs/architecture/#Cross-cutting-concerns","page":"Architecture","text":"Concurrency and locking. In-process parallelism is limited to downloads (semaphore-gated tasks). Cross-process safety uses mkpidlock around every store-mutating step (package installs, artifact installs, usage-log appends, app operations) and atomicity comes from temp-dir-plus-rename (mv_temp_dir_retries) and temp-file-plus-rename (atomic_toml_write).\nLaziness and caching. Registries parse lazily at both the index and per-package level, stay range-compressed in memory, and are cached process-wide keyed by tree hash; TOML files are cached mtime-keyed via Base.parsed_toml; the completion name list is cached in Queries.\nOffline mode. offline lives in Config; it skips registry updates and makes planners prefer installed versions (PRESERVE_ALL_INSTALLED tier). Fetch itself has no offline branch — with no server configured the server URL source simply drops out.\nError handling. All expected failures are PkgErrors thrown via pkgerror with pinned, single-call-site messages; resolver failures carry a structured ResolveLog trace. @assert is reserved for internal invariants.\nPrecompile workload. src/precompile_workload.jl drives the real API (including REPL command strings) against a hermetic synthetic registry and pre-materialized install trees in a temp depot, with IO silenced through Utils.DEFAULT_IO. Anything that makes operations touch the network or sweep the fixture depot (e.g. auto-gc) breaks precompilation; the workload scrubs all module-level caches afterwards so no session state leaks into the sysimage.","title":"Cross-cutting concerns"},{"category":"section","location":"environments/#Working-with-Environments","page":"Working with Environments","text":"An environment is an independent set of packages: a Project.toml with the direct dependencies, and next to it a Manifest.toml with the exact resolved version of everything. The pair fully determines what using/import load. This chapter covers creating environments, reproducing someone else's, temporary and shared environments, workspaces, and precompilation.","title":"Working with Environments"},{"category":"section","location":"environments/#Creating-your-own-environments","page":"Working with Environments","text":"activate a path to make it the active environment. Nothing is created until the first operation writes to it:\n\n(@v1.12) vpkg> activate MyProject\n  Activating new project at `~/MyProject`\n\n(MyProject) vpkg> st\nStatus `~/MyProject/Project.toml` (empty project)\n\n(MyProject) vpkg> add Example\n\nNow ~/MyProject contains the two environment files. The project file lists what you asked for:\n\n[deps]\nExample = \"7876af07-990d-54b4-ab0e-23690620f79a\"\n\nand the manifest records the full picture, sufficient to reconstruct the environment exactly:\n\n# This file is machine-generated - editing it directly is not advised\n\njulia_version = \"1.12.0\"\nmanifest_format = \"2.1\"\nproject_hash = \"2ca1c6c58cb30e79e021fb54e5626c96d05d5fdc\"\n\n[[deps.Example]]\ngit-tree-sha1 = \"46e44e869b4d90b96bd8ed1fdcf32244fddfb6cc\"\nuuid = \"7876af07-990d-54b4-ab0e-23690620f79a\"\nversion = \"0.5.5\"\n\nnote: Loaded packages and activation\nActivating an environment does not reload anything: packages already loaded in the session stay as they are. The environment determines what future using/import statements load.\n\nTo start Julia directly in an environment, use the --project flag:\n\n$ julia --project=.          # use the environment in the current directory\n$ julia --project=. script.jl","title":"Creating your own environments"},{"category":"section","location":"environments/#Using-someone-else's-project","page":"Working with Environments","text":"Because the manifest pins every version, reproducing an environment is two commands: activate it and instantiate it.\n\n$ git clone https://github.com/JuliaLang/Example.jl.git\n$ julia\n\n(@v1.12) vpkg> activate Example.jl\n\n(Example.jl) vpkg> instantiate\n\ninstantiate downloads and installs exactly what the manifest records (and precompiles it). If the project ships only a Project.toml, there is nothing exact to reproduce; instantiate then resolves fresh versions and creates the manifest.\n\nIf the manifest was written by a different Julia version, a warning is printed (the resolution may not be optimal for your Julia); instantiate --julia_version_strict turns that into an error, and instantiate --update_on_mismatch re-resolves instead when project and manifest disagree.","title":"Using someone else's project"},{"category":"section","location":"environments/#Returning-to-the-default-environment","page":"Working with Environments","text":"activate with no argument switches back to the default environment:\n\n(MyProject) vpkg> activate\n  Activating project at `~/.julia/environments/v1.12`\n\n(@v1.12) vpkg>\n\nand activate - toggles between the current and the previously active environment, like cd - in a shell.\n\nIf a dependency is developed at a local path, its package name is also an activation shortcut:\n\n(MyProject) vpkg> activate MyDevelopedDependency\n\nThis activates the dependency's project rather than a same-named path in the current directory. The API spelling for the previous-environment shortcut is VibePkg.activate(; prev = true).","title":"Returning to the default environment"},{"category":"section","location":"environments/#Temporary-environments","page":"Working with Environments","text":"For quickly trying a package out without touching any real environment:\n\n(@v1.12) vpkg> activate --temp\n  Activating new project at `/tmp/jl_a1B2c3`\n\n(jl_a1B2c3) vpkg> add Example\n\nThe environment lives in a fresh temporary directory that is deleted when the Julia process exits.","title":"Temporary environments"},{"category":"section","location":"environments/#Shared-environments","page":"Working with Environments","text":"A shared environment lives in the depot rather than next to any particular project, and is addressed by name:\n\n(@v1.12) vpkg> activate --shared plotting\n\n(@plotting) vpkg>\n\nThe @ in the prompt signals a shared environment; the files live in ~/.julia/environments/plotting. The default v1.12 environment is itself just a shared environment with a version-derived name.\n\nShared environments are useful for tool-style packages you want available everywhere (profilers, formatters, plotting) without adding them to every project. Note that any environment can also be put on Julia's load path to make its packages loadable alongside the active project — see the Julia manual on code loading.","title":"Shared environments"},{"category":"section","location":"environments/#Workspace-overview","page":"Working with Environments","text":"A workspace combines several projects into one development environment with a shared root manifest. It is useful for monorepos whose packages are developed together, and for keeping a package's tests, documentation, or benchmarks on the same dependency resolution. Each member retains its own Project.toml and declares its own dependencies.\n\nThe dedicated Workspaces chapter explains when to use a workspace, when separate environments are a better fit, how to lay out a monorepo, how sibling packages are path-tracked, and what --workspace changes.","title":"Workspace overview"},{"category":"section","location":"environments/#Environment-precompilation","page":"Working with Environments","text":"After an operation changes an environment, the affected packages are precompiled automatically, in parallel, so that the first using is fast:\n\n(@v1.12) vpkg> add Images\n    ...\nPrecompiling project...\n  Progress [========================================>]  85/85\n\nPrecompilation can also be run explicitly:\n\n(MyProject) vpkg> precompile\nPrecompiling project...\n  23 dependencies successfully precompiled in 36 seconds\n\nAutomatic precompilation is skipped for develop and resolve (developed code tends to be edited immediately anyway). It can be turned off entirely with the environment variable JULIA_PKG_PRECOMPILE_AUTO=0, in which case packages precompile on first load instead.\n\nTo batch several operations without precompiling after each one, use the do-block form. Automatic precompilation is suspended inside the block and one explicit precompile happens at the end:\n\nVibePkg.precompile() do\n    VibePkg.add(\"Example\")\n    VibePkg.develop(\"JSON\")\n    VibePkg.up(\"HTTP\")\nend\n\nThe progress display distinguishes packages being compiled, successful packages, packages which decline precompilation with __precompile__(false), and failures. A package that failed during automatic precompilation is skipped on later automatic runs until its inputs change; an explicit precompile retries it. Pass package names to the API (VibePkg.precompile([\"Example\"])) to limit an explicit run to those packages and their dependency closure, or use strict = true to make any failure throw.\n\nOn Julia versions whose precompiler supports background detaching, interactive progress can be detached with d; compilation continues while the REPL returns. Press ? while the progress display is active to see the controls provided by that Julia version.\n\nIf a new version of a package is installed while an older one is loaded in the session, the new version is still precompiled correctly against the loaded world — but you need a fresh session to actually use it.","title":"Environment precompilation"},{"category":"section","location":"environment-variables/#env-vars","page":"Environment variables","text":"Every environment variable VibePkg reads, in one place. The linked sections describe each feature in context.","title":"Environment variables"},{"category":"section","location":"environment-variables/#Package-servers-and-downloads","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_SERVER The package server to download packages, registries, and artifacts from. Default https://pkg.julialang.org; the empty string disables package servers entirely and forces fetching from the original hosts.\nJULIA_PKG_SERVER_* Any variable with this prefix is forwarded to the package server as a Julia-* HTTP header (JULIA_PKG_SERVER_REGISTRY_PREFERENCE → Julia-Registry-Preference), so servers can offer configuration of their own.\nJULIA_PKG_SERVER_REGISTRY_PREFERENCE The registry flavor served by the package server: conservative (default) or eager. Applied server-side on registry update; shown by Registry.status().\nJULIA_PKG_OFFLINE true enables offline mode: no registry updates, and resolution only considers installed versions.\nJULIA_PKG_CONCURRENT_DOWNLOADS Number of parallel package/artifact downloads. Default 8.\nJULIA_PKG_USE_CLI_GIT true uses the command-line git executable instead of the built-in LibGit2 for git operations.","title":"Package servers and downloads"},{"category":"section","location":"environment-variables/#env-registries","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_UNPACK_REGISTRY true stores server-provided registries unpacked as a directory tree instead of as a packed tarball.\nJULIA_PKG_GEN_REG_FMT_CHECK false silences the once-per-session nudge to reinstall a git/unpacked General registry in the faster packed form. Default true.","title":"Registries"},{"category":"section","location":"environment-variables/#Resolution","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_RESOLVE_MAX_TIME Resolver time budget in seconds. Default 300.\nJULIA_PKG_RESOLVE_ACCURACY Resolver effort multiplier (integer ≥ 1, default 1): higher values try harder on conflict-heavy graphs at the cost of time. JULIA_PKGRESOLVE_ACCURACY is the legacy spelling.\nJULIA_PKG_PRESERVE_TIERED_INSTALLED true makes tiered_installed the default preserve tier for add, preferring already-installed versions.","title":"Resolution"},{"category":"section","location":"environment-variables/#Precompilation","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_PRECOMPILE_AUTO 0/false disables the automatic precompilation after environment-changing operations.\nJULIA_NUM_PRECOMPILE_TASKS Number of parallel precompilation tasks (honored by Base.Precompilation).\nJULIA_PKG_DISALLOW_PKG_PRECOMPILATION true makes precompiling VibePkg itself (or its REPL extension) an error. Useful when working on VibePkg, to catch an accidental recompile of the package manager mid-session.","title":"Precompilation"},{"category":"section","location":"environment-variables/#Garbage-collection","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_GC_AUTO false disables the automatic weekly gc that otherwise runs after up/pin/free/rm.","title":"Garbage collection"},{"category":"section","location":"environment-variables/#env-artifacts","page":"Environment variables","text":"Variable Effect\nJULIA_PKG_IGNORE_HASHES 1 downgrades artifact tree-hash mismatches to a warning and installs anyway; 0 forbids that even on Windows, where it otherwise applies automatically for users who cannot create symlinks. See Installation and verification.","title":"Artifacts"},{"category":"section","location":"environment-variables/#Paths-and-tools","page":"Environment variables","text":"Variable Effect\nJULIA_DEPOT_PATH The depot stack — where packages, registries, artifacts, and logs live. See Depots.\nJULIA_PKG_DEVDIR Where develop clones packages. Default ~/.julia/dev.\nJULIA_APPS_JULIA_CMD The julia binary app shims launch. Default: the julia that installed the app.","title":"Paths and tools"},{"category":"section","location":"environment-variables/#Output","page":"Environment variables","text":"Variable Effect\nCI true disables the interactive (fancy) progress output, as do non-TTY streams.","title":"Output"},{"category":"section","location":"creating-packages/#Creating-Packages","page":"Creating Packages","text":"A package is a project with a name, a uuid, and a version in its Project.toml, and a src/PackageName.jl that defines the module PackageName. Packages are the unit of code that can be depended on, versioned, and registered.","title":"Creating Packages"},{"category":"section","location":"creating-packages/#Generating-a-package-skeleton","page":"Creating Packages","text":"generate creates the minimal structure:\n\n(@v1.12) vpkg> generate HelloWorld\n  Generating  project HelloWorld:\n    HelloWorld/Project.toml\n    HelloWorld/src/HelloWorld.jl\n\ngiving\n\nHelloWorld/\n├── Project.toml\n└── src\n    └── HelloWorld.jl\n\nThe project file has the three package-defining fields (the author is taken from your git configuration):\n\nname = \"HelloWorld\"\nuuid = \"b4cd1eb8-1e24-11e8-3319-93036a3eb9f3\"\nversion = \"0.1.0\"\nauthors = [\"Some One <someone@email.com>\"]\n\nand the source file defines the module:\n\nmodule HelloWorld\n\ngreet() = print(\"Hello World!\")\n\nend # module\n\nActivate the package's environment to use and work on it:\n\n(@v1.12) vpkg> activate ./HelloWorld\n\njulia> import HelloWorld\n\njulia> HelloWorld.greet()\nHello World!\n\nnote: Note\ngenerate is intentionally minimal. For registered packages you will also want tests, CI configuration, licensing, and documentation — PkgTemplates.jl generates all of that.","title":"Generating a package skeleton"},{"category":"section","location":"creating-packages/#Adding-dependencies","page":"Creating Packages","text":"With the package's environment active, add records dependencies in the package's own [deps] table:\n\n(HelloWorld) vpkg> add Random JSON\n\nBecause HelloWorld is a package project (its Project.toml has both a name and UUID), add also creates a [compat] entry for each newly added direct dependency, beginning at the version that was resolved. Existing compat entries are left unchanged. Treat the generated entry as a useful starting point: before publishing, adjust it to describe the full range your package actually supports. See Compatibility entries created by add.\n\nThey are then loadable from the package:\n\nmodule HelloWorld\n\nimport Random\nimport JSON\n\ngreet() = print(\"Hello World!\")\ngreet_alien() = print(\"Hello \", Random.randstring(8))\n\nend # module","title":"Adding dependencies"},{"category":"section","location":"creating-packages/#Defining-a-public-API","page":"Creating Packages","text":"Names that are part of your package's supported surface should be marked, so users can tell API from internals:\n\nexport greet makes greet available with using HelloWorld and marks it public;\npublic greet_alien marks it public without exporting it (users call HelloWorld.greet_alien()).\n\nEverything else is internal by convention, and may change in any release. Changes to the public API are what drive your version number: breaking changes require a new major version (or a new minor version while below 1.0) — see Compatibility.","title":"Defining a public API"},{"category":"section","location":"creating-packages/#Adding-tests","page":"Creating Packages","text":"Tests live in test/runtests.jl and run with vpkg> test in a sandboxed environment. Test-only dependencies are best declared with a workspace: give the test directory its own project that develops the package by path. This is one application of the general workspace model; see Workspaces for monorepos, sibling packages, and guidance on choosing a shared versus an independent resolution.\n\nHelloWorld/Project.toml declares the workspace:\n\nname = \"HelloWorld\"\nuuid = \"b4cd1eb8-1e24-11e8-3319-93036a3eb9f3\"\nversion = \"0.1.0\"\n\n[workspace]\nprojects = [\"test\"]\n\nand test/Project.toml holds the test dependencies plus the package itself via [sources]:\n\n[deps]\nTest = \"8dfed614-e22c-5e08-85e1-65c5234f0b40\"\nHelloWorld = \"b4cd1eb8-1e24-11e8-3319-93036a3eb9f3\"\n\n[sources]\nHelloWorld = {path = \"..\"}\n\nThat file is created for you by activating the test project and adding to it:\n\n(HelloWorld) vpkg> activate ./test\n\n(test) vpkg> dev .\n\n(test) vpkg> add Test\n\nSince the workspace shares one manifest, the package and its tests always agree on dependency versions. The same pattern extends to docs and benchmarks: projects = [\"test\", \"docs\", \"benchmarks\"].\n\nAny package imported directly by the tests must appear in test/Project.toml; the parent package's dependencies are not implicitly inherited by the test project.","title":"Adding tests"},{"category":"section","location":"creating-packages/#Separate-test-environments","page":"Creating Packages","text":"A test/Project.toml can also be used without putting test in the root workspace. Give it the same [deps] and [sources] entry shown above. It then has its own test/Manifest.toml, resolved independently from the package's environment:\n\n$ julia --project=test\n\njulia> using HelloWorld, Test\n\njulia> include(\"test/runtests.jl\")\n\nThis is useful when the test environment deliberately needs a different resolution. A workspace is usually preferable because its one manifest makes incompatibilities between the package and its tests visible immediately.","title":"Separate test environments"},{"category":"section","location":"creating-packages/#Legacy:-target-based-test-dependencies","page":"Creating Packages","text":"Older packages declare test dependencies in [extras] with a test target:\n\n[extras]\nTest = \"8dfed614-e22c-5e08-85e1-65c5234f0b40\"\n\n[targets]\ntest = [\"Test\"]\n\nThis still works, but the workspace setup above is preferred — it gives the tests a real project file and a shared, committed resolution.\n\nvpkg> add --extra Test adds the [extras] entry; the target still needs to be declared in the file.","title":"Legacy: target-based test dependencies"},{"category":"section","location":"creating-packages/#Adding-a-build-step","page":"Creating Packages","text":"A deps/build.jl script runs automatically the first time the package is installed, and on vpkg> build:\n\n# deps/build.jl\nprintln(\"I am being built...\")\n\n(HelloWorld) vpkg> build\n    Building HelloWorld → `.../scratchspaces/.../build.log`\n\nA failing build prints the log and errors the install. Build steps are the legacy mechanism for fetching binaries; new packages should almost always use Artifacts instead, and per-user configuration should use Preferences.jl.\n\nwarning: Warning\nThe build step must not modify the package's own directory: installed packages are read-only and shared between environments. Write to a scratchspace (Scratch.jl) instead.\n\nThe same immutability rule applies to tests: they may run against an installed, read-only source tree. Put temporary output in a temporary directory or a scratchspace, not under the package directory.","title":"Adding a build step"},{"category":"section","location":"creating-packages/#Compatibility-on-dependencies","page":"Creating Packages","text":"Every dependency of a package should have a [compat] entry — registries require it, and it is what makes resolution reproducible and safe:\n\n[compat]\nJSON = \"0.21\"\njulia = \"1.12\"\n\nSee Compatibility for the specifier syntax.","title":"Compatibility on dependencies"},{"category":"section","location":"creating-packages/#Weak-dependencies-and-extensions","page":"Creating Packages","text":"A package can ship code that only activates when some other package is loaded, without depending on it unconditionally. The optional package is declared as a weak dependency, and the conditional code as an extension module:\n\n(Plotting) vpkg> add --weak Contour\n\nname = \"Plotting\"\nversion = \"0.1.0\"\nuuid = \"...\"\n\n[weakdeps]\nContour = \"d38c429a-6771-53c6-b99e-75d170b6e991\"\n\n[extensions]\n# name of the extension => weak dependencies it needs\nContourExt = \"Contour\"\n\n[compat]\nContour = \"0.6.2\"\n\nThe extension lives in ext/ContourExt.jl:\n\nmodule ContourExt # Same name as declared in Project.toml\n\nusing Plotting, Contour\n\nfunction Plotting.plot(c::Contour.ContourCollection)\n    # plotting a contour\nend\n\nend # module\n\nAn extension with more than one file uses a directory instead: ext/ContourExt/ContourExt.jl plus whatever it includes. The name is free to choose (output always shows it together with its parent package), and an extension can require several weak dependencies at once: ExtName = [\"PkgA\", \"PkgB\"].","title":"Weak dependencies and extensions"},{"category":"section","location":"creating-packages/#How-extensions-behave","page":"Creating Packages","text":"ContourExt loads automatically — in the background, like precompilation — once both Plotting and Contour are loaded into the same session, and it precompiles like a package. Users who never load Contour pay nothing for the extension's existence. Weak dependencies need [compat] entries like regular ones, but are not installed unless something else brings them in.\n\nThe archetypal extension adds methods to functions of its parent package, as Plotting.plot(c::Contour.ContourCollection) does above. The parent can even declare the function with zero methods — function plot end — leaving all implementations to extensions. (Strictly speaking the extension owns neither Plotting.plot nor Contour.ContourCollection, which would normally be type piracy; an extension is considered part of its parent, so extending the parent's functions is fine.)\n\nAn extension can also define new names — types, functions — but those live in the extension module, not in the parent. When the parent needs them, it can fetch the module once the extension has loaded:\n\next = Base.get_extension(@__MODULE__, :ContourExt)\nif ext !== nothing\n    ContourPlotType = ext.ContourPlotType\nend\n\nThird-party packages should not reach into someone else's extension this way — anything meant for external consumption belongs in the parent package's public API.\n\nvpkg> status --extensions shows the extensions of the environment and whether each is loaded.\n\nnote: Manifests from older Julia versions\nA manifest generated by a Julia version that predates extensions does not contain the metadata needed to load them. Regenerate the manifest with a Julia version that supports extensions before relying on them.","title":"How extensions behave"},{"category":"section","location":"creating-packages/#Testing-extensions","page":"Creating Packages","text":"The package's own tests usually want the extensions active. With the workspace test setup from Adding tests that is just a matter of adding the weak dependencies to test/Project.toml — loading them in the test session triggers the extensions like anywhere else.\n\nExtensions replaced the callback-based Requires.jl pattern, with the advantage that extension code precompiles and participates in resolution via [compat].","title":"Testing extensions"},{"category":"section","location":"creating-packages/#Supporting-Julia-versions-without-extensions","page":"Creating Packages","text":"If the package must also run on Julia versions older than 1.9, keep the modern [weakdeps]/[extensions] declaration and provide an old-Julia bridge. A package migrating from Requires.jl can keep Requires in both [deps] and [weakdeps], then install its @require callback only when Base.get_extension is not defined. Another migration path is to list the optional package in both [deps] and [weakdeps] and include the extension file directly on old Julia versions. New Julia versions treat the overlapping dependency as weak and use the extension normally.\n\nKeep these branches guarded with @static checks so unsupported code is not lowered on the wrong Julia version, and test both paths. Packages that merely support old Julia without providing the optional feature there need no bridge; they can leave the extension unavailable on those releases.","title":"Supporting Julia versions without extensions"},{"category":"section","location":"creating-packages/#Best-practices","page":"Creating Packages","text":"An installed package is read-only, shared by every environment that resolves to that version, and possibly bundled into a system image or a system-wide depot — so a package should never assume its own directory is writable, or even that its location is stable. Concretely:\n\nData files: ship or download them as Artifacts rather than opening paths relative to @__DIR__. Precompilation bakes @__DIR__ in, so path-relative loading breaks when the package is relocated.\nCaches and other mutable state: use a scratchspace via Scratch.jl — per-package mutable storage that is garbage-collected when the package goes away. Important user-created data should instead go to a user-chosen path that VibePkg does not manage.\nConfiguration: record it with Preferences.jl, which stores per-project preferences readable at load time, instead of writing config files into the package directory.","title":"Best practices"},{"category":"section","location":"creating-packages/#Package-naming-rules","page":"Creating Packages","text":"For a package you intend to register: use a descriptive UpperCamelCase name that says what the package does rather than a joke or an acronym, avoid putting \"Julia\" or \"Ju\" in the name, and keep it distinct enough from existing package names not to be confused with them (the General registry enforces a similarity check). Packages wrapping an external library conventionally take its name — CPLEX.jl wraps CPLEX — and pure-binary packages generated from build recipes end in _jll.","title":"Package naming rules"},{"category":"section","location":"creating-packages/#Registering-and-releasing","page":"Creating Packages","text":"Registering a package in the General registry, and releasing new versions of it, is done from the package's repository with Registrator.jl; most packages also use TagBot to tag releases. Bump version in Project.toml according to the public-API rules above, and register. Once the registry PR merges, the new version is available to add everywhere.","title":"Registering and releasing"},{"category":"section","location":"repl/#REPL-mode","page":"The Pkg REPL mode","text":"VibePkg comes with a REPL mode of its own. From the Julia REPL, press ] at the start of an empty line to enter it. The prompt changes to\n\n(MyProject) vpkg>\n\nwhere the part in parentheses shows the environment you are operating on: the name of the active project's directory, or @v1.12-style names for shared environments. The mode is sticky — after a command runs you stay in it. Press backspace (or Ctrl-C) on an empty line to get back to the julia> prompt.\n\nSeveral commands can be chained on one line by separating them with ;:\n\n(@v1.12) vpkg> add Example; status\n\nTab completion works throughout: command names at the start of the line, registered package names after add and develop, names of packages already in the environment after commands like rm, up, and why, option names after --, and directory paths after activate.\n\nTyping ? (or help) shows a summary of all commands, and ? add (or ?add) shows the full help for a single command.\n\nnote: Note\nWhile VibePkg is developed alongside Pkg, its REPL mode is installed explicitly with VibePkg.REPLMode.install_repl!(; key = ']') so it can coexist with (or replace) Pkg's own ] mode.","title":"The Pkg REPL mode"},{"category":"section","location":"repl/#Package-specification-syntax","page":"The Pkg REPL mode","text":"Commands that accept packages (add, develop, pin, …) understand a small grammar for each word:\n\nForm Meaning\nExample a registered package, by name\nExample.jl the .jl suffix is stripped; same as above\nExample@0.5 with a version (any VersionSpec, see Compatibility)\nExample=7876af07-... name and UUID (disambiguates duplicate names)\n7876af07-... a bare UUID\nExample#master a registered package, tracked by git revision\nhttps://github.com/... a git URL, optionally with #revision or :subdir\n./LocalPkg, ~/dev/Pkg a local path (anything containing a slash), optionally with :subdir\n\nURLs and paths are only valid for add and develop; the other commands refer to packages that are already part of the environment. Revisions (#rev) are valid for add but not for develop — a developed package always tracks whatever is checked out at its path.\n\nGitHub /tree/<rev>, /commit/<rev>, and /pull/<number> URLs are recognized directly and converted to the corresponding repository revision.\n\nWords may be quoted with ' or \" when a path contains spaces.","title":"Package specification syntax"},{"category":"section","location":"repl/#Command-reference","page":"The Pkg REPL mode","text":"","title":"Command reference"},{"category":"section","location":"repl/#add","page":"The Pkg REPL mode","text":"add [--preserve=<opt>] [-w|--weak] [-e|--extra]\n    pkg[=uuid] [@version] [#rev] | url [#rev] [:subdir] | path ...\n\nAdd packages to the project. Registered names may carry a version (@0.5), a UUID (=uuid), or a git revision (#master); URLs and local paths are tracked as git sources. --preserve picks the resolve tier for the packages already in the environment: installed, all, direct, semver, none, tiered_installed, or tiered (the default). See Preserve tiers for what they mean. --weak and --extra record registered packages in the project's [weakdeps] or [extras] table without resolving or installing them.\n\nExamples\n\nvpkg> add Example\nvpkg> add Example@0.5\nvpkg> add Example#master\nvpkg> add Example=7876af07-990d-54b4-ab0e-23690620f79a\nvpkg> add https://github.com/JuliaLang/Example.jl#master\nvpkg> add https://github.com/timholy/SnoopCompile.jl:SnoopCompileCore\nvpkg> add --weak ChainRulesCore\nvpkg> add Example JSON StaticArrays","title":"add"},{"category":"section","location":"repl/#develop,-dev","page":"The Pkg REPL mode","text":"develop [--preserve=<opt>] [--shared|--local] pkg|path\n\nTrack a package by source path so that edits to it take effect immediately (after a re-resolve picks up dependency changes). A registered name is cloned into the shared development directory (--shared, the default, usually ~/.julia/dev) or into the project's own dev/ folder (--local); a path is used as-is.\n\nExamples\n\nvpkg> develop Example\nvpkg> develop --local Example\nvpkg> develop ./MyLocalPackage","title":"develop, dev"},{"category":"section","location":"repl/#remove,-rm","page":"The Pkg REPL mode","text":"rm [-p|--project] [-m|--manifest] pkg ...\nrm [-p|--project] [-m|--manifest] --all\n\nRemove packages. In project mode (the default) the packages are dropped as direct dependencies and anything no longer needed disappears from the manifest. In manifest mode the packages are removed from the manifest together with everything that depends on them. --all removes all packages in scope.","title":"remove, rm"},{"category":"section","location":"repl/#update,-up","page":"The Pkg REPL mode","text":"up [-p|--project] [-m|--manifest] [--major|--minor|--patch|--fixed]\n   [--preserve=<all|direct|none>] [--workspace] [pkg ...]\n\nUpdate packages within the given level (--major is the default). With no arguments the whole environment updates; with names, only those packages move while the rest is held according to --preserve. --manifest seeds every manifest package rather than just direct dependencies, and --workspace also updates the dependencies of all workspace members.\n\nExamples\n\nvpkg> up\nvpkg> up Example\nvpkg> up --minor Example","title":"update, up"},{"category":"section","location":"repl/#pin","page":"The Pkg REPL mode","text":"pin pkg[@version] ...\npin [--workspace] --all\n\nPin packages at their current version, or at the given version. A pinned package never moves — not even for up — until it is freed. --all pins every package.\n\nExamples\n\nvpkg> pin Example\nvpkg> pin Example@0.5.1\nvpkg> pin --all","title":"pin"},{"category":"section","location":"repl/#free","page":"The Pkg REPL mode","text":"free pkg ...\nfree [--workspace] --all\n\nUndo a pin, develop, or repo-tracking (#rev): return the packages to tracking the registry. --all frees everything freeable.","title":"free"},{"category":"section","location":"repl/#status,-st","page":"The Pkg REPL mode","text":"status [-p|--project] [-m|--manifest] [-d|--diff] [-o|--outdated]\n       [--deprecated] [-c|--compat] [-e|--extensions] [--workspace] [pkg ...]\n\nShow the environment. Project mode (default) lists the direct dependencies; --manifest lists everything in the manifest. In the listing, ⌃ marks packages with a newer version available and ⌅ packages held back from their newest version by compatibility constraints; --outdated explains what is holding each one back. --diff shows the change relative to the last git committed version of the environment files, --compat shows the declared compat entries, --extensions the state of package extensions, --deprecated flags packages marked deprecated in the registry, and --workspace includes every workspace member's dependencies. Package names filter the listing to matching entries; manifest-mode matches include their dependencies.","title":"status, st"},{"category":"section","location":"repl/#undo-/-redo","page":"The Pkg REPL mode","text":"undo\nredo\n\nRevert the active environment to its previous state, and reapply an undone change. Each environment keeps its own undo stack for the session.","title":"undo / redo"},{"category":"section","location":"repl/#instantiate","page":"The Pkg REPL mode","text":"instantiate [-p|--project] [-m|--manifest] [-v|--verbose] [--workspace]\n            [--julia_version_strict] [-u|--update_on_mismatch]\n\nMake the environment ready to use: download and install exactly what the manifest records (and precompile it). This is the command to run after checking out a project that ships a Manifest.toml. --project ignores the manifest and resolves from the project file instead; --verbose shows build output; --julia_version_strict turns manifest version-check warnings into errors; and --update_on_mismatch falls back to up when the manifest does not match the project.","title":"instantiate"},{"category":"section","location":"repl/#resolve","page":"The Pkg REPL mode","text":"resolve\n\nReconcile the manifest with the project — for example after a developed dependency changed its own dependencies — while keeping installed versions in place as far as possible.","title":"resolve"},{"category":"section","location":"repl/#precompile","page":"The Pkg REPL mode","text":"precompile [--strict] [--timing] [--workspace] [pkg ...]\n\nPrecompile all packages in the environment, or only the named packages and their dependency closure. --strict makes every compile failure an error, --timing reports per-package compile time, and --workspace includes all workspace members. Packages are auto-precompiled after operations that change the environment, so running this by hand is rarely needed (see Configuration for how to turn auto-precompilation off).","title":"precompile"},{"category":"section","location":"repl/#test","page":"The Pkg REPL mode","text":"test [--coverage] [pkg ...]\n\nRun package tests in a sandboxed environment built from the package's test setup. With no arguments the active project itself is tested. --coverage enables collection of coverage statistics.","title":"test"},{"category":"section","location":"repl/#build","page":"The Pkg REPL mode","text":"build [-v|--verbose] [pkg ...]\n\nRun the deps/build.jl script of the given packages (dependencies first), or of every package with a build script when no names are given. --verbose shows the build output directly instead of only logging it to a file.","title":"build"},{"category":"section","location":"repl/#gc","page":"The Pkg REPL mode","text":"gc [-v|--verbose] [--all]\n\nGarbage-collect the depot: delete packages, artifacts, repo clones, and scratchspaces that are no longer reachable from any environment that has been used. Unreachable content is deleted immediately. By default only the first depot in the depot stack is swept; --all sweeps all of them. --verbose lists what is kept and what is deleted.","title":"gc"},{"category":"section","location":"repl/#activate","page":"The Pkg REPL mode","text":"activate [path]\nactivate --shared name\nactivate --temp\nactivate -\n\nSet the environment commands operate on. With no argument, the default (@v#.#) environment; with a path, the project at that path (created on first mutation if it doesn't exist). --shared name activates the named environment in the depot's environments folder, creating it if needed. --temp creates and activates a temporary environment that is deleted when the Julia process exits. activate - toggles between the current and previously active environments. If a name identifies a path-developed dependency, its project is activated.\n\nExamples\n\nvpkg> activate .\nvpkg> activate MyProject\nvpkg> activate --shared plotting\nvpkg> activate --temp\nvpkg> activate -\nvpkg> activate","title":"activate"},{"category":"section","location":"repl/#generate","page":"The Pkg REPL mode","text":"generate path\n\nCreate a minimal package skeleton: a Project.toml with a name, a fresh UUID, and an author, plus src/<Name>.jl with a module definition.","title":"generate"},{"category":"section","location":"repl/#compat","page":"The Pkg REPL mode","text":"compat [pkg] [version]\ncompat --current\n\nWith no arguments, show the project's [compat] table. With one argument, remove that entry. With two, set the entry and re-check the environment against it. --current fills in missing compat entries from the currently resolved versions.\n\nExamples\n\nvpkg> compat\nvpkg> compat Example 0.5\nvpkg> compat Example\nvpkg> compat --current","title":"compat"},{"category":"section","location":"repl/#why","page":"The Pkg REPL mode","text":"why [--workspace] pkg ...\n\nShow the dependency paths that lead from the project to the given packages as a tree — the answer to \"why is this in my manifest?\". Branches terminating in a queried package are marked with a colored ▶; a package whose sub-tree has already been printed is shown as Name (*) instead of being expanded again.","title":"why"},{"category":"section","location":"repl/#registry","page":"The Pkg REPL mode","text":"registry add [url]\nregistry update\nregistry status\n\nManage registries: add installs a registry (with no URL, the default registries — typically General), update refreshes all installed registries, and status lists them. See Registries.","title":"registry"},{"category":"section","location":"repl/#app","page":"The Pkg REPL mode","text":"app add pkg\napp develop path\napp rm name\napp update [name]\napp status\n\nManage applications: Julia packages installed so that their command-line entry points become executables on your PATH. app update refreshes every installed app, or only the named package/app. See Apps.","title":"app"},{"category":"section","location":"#VibePkg.jl","page":"VibePkg.jl","text":"Welcome to the documentation for VibePkg, a package manager for the Julia programming language. VibePkg is a ground-up rewrite of Pkg.jl that keeps Pkg's user-facing behavior, file formats, and public API, while rebuilding the internals around immutable values and a strict separation between planning and execution.","title":"VibePkg.jl"},{"category":"section","location":"#What-VibePkg-does","page":"VibePkg.jl","text":"VibePkg is built around environments: independent sets of packages that can be local to a single project or shared between projects. The set of packages in an environment is recorded in two TOML files — a project file with your direct dependencies and their compatibility constraints, and a manifest file with the exact version of every package in the full dependency graph. A manifest makes an environment reproducible: checking out a project and running instantiate gives you exactly the package versions its author used.\n\nBecause environments are just files, different projects can use different — and even incompatible — versions of the same packages without interfering with each other. The actual package installations live in a shared depot on disk and are reused between environments, so having many environments is cheap.\n\nVibePkg can be used in two ways:\n\nthrough a dedicated REPL mode, entered by pressing ] at the Julia prompt — the most convenient way to use it interactively:\n(@v1.12) vpkg> add Example\nthrough a normal functional API, which is the better fit for scripts and programmatic use:\njulia > using VibePkg\n\njulia > VibePkg.add(\"Example\")\n\nNearly every REPL command corresponds to one API function, and the two are documented together throughout.","title":"What VibePkg does"},{"category":"section","location":"#Notable-capabilities","page":"VibePkg.jl","text":"Beyond the usual add, update, and remove workflow, VibePkg includes:\n\nworkspaces, which give monorepos and related projects one shared resolution while keeping each member's dependencies project-local;\nrecursive [sources], so private or unregistered packages can describe where their own unregistered dependencies come from;\npackage-author conveniences such as compat entries created by add, weak dependencies, and extensions;\na rich status view for finding outdated, constrained, deprecated, changed, and extension-providing packages; and\ntransactional environment changes with undo and redo, plus isolated installation of command-line programs through Apps.","title":"Notable capabilities"},{"category":"section","location":"#Relation-to-Pkg.jl","page":"VibePkg.jl","text":"VibePkg deliberately behaves like Pkg: the same commands, the same Project.toml/Manifest.toml formats (written byte-identically), the same registries, package servers, and depot layout. If you know Pkg, you know VibePkg. The differences are internal architecture, a number of fixed bugs, and a few sharpened behaviors.","title":"Relation to Pkg.jl"},{"category":"section","location":"#Where-to-go-from-here","page":"VibePkg.jl","text":"Getting Started — a quick tour: installing packages, using them, and working with environments.\nManaging Packages — adding, updating, pinning, developing, and removing packages in depth.\nWorking with Environments — project-local environments, shared environments, and temporary environments.\nWorkspaces — one shared resolution for monorepos, sibling packages, tests, documentation, and other projects developed together.\nCompatibility — declaring which versions of your dependencies your project works with.\nRegistries and Artifacts — where packages come from and how binary data is handled.\nApps — installing packages as command-line executables.\nGlossary, Project and Manifest files, REPL mode reference, and the API Reference — the reference material.","title":"Where to go from here"}]
}
